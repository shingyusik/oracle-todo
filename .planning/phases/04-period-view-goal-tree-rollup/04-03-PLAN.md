---
phase: 04-period-view-goal-tree-rollup
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - todo-engine/tests/integration/period_view.rs
autonomous: true
requirements: [VIEW-03, VIEW-04]
must_haves:
  truths:
    - "The Persistent SQLite store builds the same period-view subtree as the service contract over a real temp DB (SC1/VIEW-03)."
    - "InMemory and Persistent stores produce identical PeriodView trees for the same seed (SC4/CORE-03, D-11), compared by structure-capturing stable keys, never raw ids."
    - "A store-level-injected cyclic/orphaned parent_id terminates safely on BOTH stores: anomaly_count is bumped and period_view returns Ok, never hangs or Errs (SC3/D-08/D-09)."
    - "period_view over the Persistent store writes no audit event (side-effect-free, SC3/CORE-03)."
  artifacts:
    - path: "todo-engine/tests/integration/period_view.rs"
      provides: "Persistent tree test, parity_in_memory_vs_persistent, SC3 store-level cycle/orphan/depth anomaly tests, side-effect-free test"
      contains: "parity_in_memory_vs_persistent"
  key_links:
    - from: "todo-engine/tests/integration/period_view.rs::parity_in_memory_vs_persistent"
      to: "TodoService::period_view (both InMemory and Persistent)"
      via: "tree_keys() stable-key compare of both store outputs"
      pattern: "parity_in_memory_vs_persistent"
    - from: "todo-engine/tests/integration/period_view.rs (SC3 fixtures)"
      to: "raw SQLite rows / hand-built InMemory map with a back-edge"
      via: "store-level injection bypassing validate_goal_nesting"
      pattern: "cycle"
---

<objective>
Lock the Persistent SQL path and cross-store parity with integration tests, and prove SC3 safety on cyclic/orphaned legacy data that the validating service API cannot create. Add: a persistent-store subtree test, the mandatory `parity_in_memory_vs_persistent` test (D-11), SC3 anomaly tests fed by store-level-injected cycles/orphans/over-depth chains, and a persistent side-effect-free test.

Purpose: D-10 split the load paths (Persistent SQL CTE vs InMemory Rust walk), so parity is no longer automatic and must be explicitly tested (D-11) — this is the mandatory deliverable that proves the two loaders yield the same tree. SC3's cycle/depth guard cannot be exercised through the normal service API because goal creation rejects cycles at create time (goal.rs:85), so the anomaly fixtures must be injected at the store level (raw SQLite rows for Persistent, a hand-built InMemory map with a back-edge) — this plan owns that low-level fixture path.
Output: All persistent + parity + SC3-anomaly + side-effect-free tests appended to `tests/integration/period_view.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-period-view-goal-tree-rollup/04-CONTEXT.md
@.planning/phases/04-period-view-goal-tree-rollup/04-RESEARCH.md
@.planning/phases/04-period-view-goal-tree-rollup/04-PATTERNS.md
@.planning/phases/04-period-view-goal-tree-rollup/04-01-SUMMARY.md
@.planning/phases/04-period-view-goal-tree-rollup/04-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan introduces test functions only (no production symbols):
- `persistent_service()` helper (copied from date_view.rs), `persistent_period_view_builds_subtree`, `parity_in_memory_vs_persistent`, `cycle_is_severed_no_error`, `orphan_parent_no_error`, `depth_cap_truncates_persistent`, `period_view_is_side_effect_free_persistent`, plus store-level cyclic/orphan fixture helpers.

Reuses from Plans 01/02 (do NOT redefine): `period_view`, `PeriodView`/`GoalNode`, `tree_keys()`, `goal`/`open_task`/`seed_goal_tree` helpers (Plan 01), `load_period_subtree` (Plan 02).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Persistent subtree test + parity_in_memory_vs_persistent + persistent side-effect-free</name>
  <read_first>
    - todo-engine/tests/integration/period_view.rs (the file being modified; the Plan 01 `tree_keys()`, `goal`, `open_task`, `seed_goal_tree` helpers to reuse)
    - todo-engine/tests/integration/date_view.rs (`persistent_service()`:10-17 to copy verbatim — NOT shared outside e2e; `parity_in_memory_vs_persistent`:159-174 pattern; side-effect-free via `events().len()`:141-152)
    - todo-engine/tests/integration/goal_view.rs (`persistent_service()`:20-27 alternate copy; goal seed via the real service API)
    - todo-engine/src/infrastructure/sqlite/mod.rs (`connect`:13, `SqliteTodoRepository::new`:22, `init_schema` re-export — the persistent test setup)
    - todo-engine/src/application/service/queries.rs (the `period_view` contract + `PeriodView`/`GoalNode` shapes from Plans 01/02)
    - .planning/phases/04-period-view-goal-tree-rollup/04-02-SUMMARY.md (confirm the CTE applies the D-07 predicate identically so parity holds on terminal items)
  </read_first>
  <action>
    Append to `tests/integration/period_view.rs`. Copy `persistent_service()` verbatim from date_view.rs:10-17 (stands up a temp SQLite home, `connect` + `init_schema` + `SqliteTodoRepository::new` + `TodoService::persistent`).

    `persistent_period_view_builds_subtree` (SC1/VIEW-03 over real SQLite): run `seed_goal_tree` (Plan 01) against a `persistent_service()`, call `period_view(Horizon::Month, "2026-06-01")`, and assert via `tree_keys()` that roots = the exact-match month goals with the nested week child and the scheduled + unscheduled tasks present — i.e. the SQL CTE path produces the documented tree shape. Include at least one terminal goal in this seed (complete a child goal via the service API) and assert it is STILL traversed/present (D-07: terminal goals kept) while a terminal task is excluded (open-only) — proving the CTE's asymmetric visibility predicate.

    `parity_in_memory_vs_persistent` (SC4/CORE-03/D-11 — MANDATORY): seed the IDENTICAL `seed_goal_tree` into both `TodoService::in_memory()` and a `persistent_service()`. The seed MUST include a TERMINAL task (e.g. complete a task linked under a goal via the service API) AND a live (open) task under the same goal, so the D-07 task-visibility predicate is exercised on BOTH stores. Call `period_view(Horizon::Month, "2026-06-01")` on each; assert `tree_keys(&mem_view) == tree_keys(&disk_view)` (the structure-capturing `(title, depth, kind)` key — NEVER raw ids, since in-memory uses `goal_000001`/`task_000001` and persistent uses UUIDs). Because the key sets are equal AND the terminal task is open-only-filtered, the terminal task is absent in BOTH stores by exactly the same rule — assert explicitly that the terminal task's title appears in NEITHER `tree_keys` result and the open task appears in BOTH (the cross-store D-07 absence-parity assertion: the two loaders agree on which tasks exist). Also assert `mem_view.anomaly_count == disk_view.anomaly_count` for the clean seed (both 0).

    `period_view_is_side_effect_free_persistent` (SC3/CORE-03): over a `persistent_service()` seeded with a small tree, capture `service.events().len()`, call `period_view`, assert the count is unchanged (mirror date_view.rs:141-152) — proving the SQL load writes nothing.
  </action>
  <verify>
    <automated>cd todo-engine && cargo test --test integration period_view</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test integration period_view` is green including the three new tests.
    - `parity_in_memory_vs_persistent` exists, seeds the same fixture (INCLUDING a terminal task + a live task under the same goal) into both stores, and asserts equality via `tree_keys()` (structure-capturing key) AND equal `anomaly_count` — `grep -c 'parity_in_memory_vs_persistent' todo-engine/tests/integration/period_view.rs` returns 1.
    - The parity test asserts the seeded terminal task's title is absent from BOTH `tree_keys` results and the live task's title is present in BOTH — the cross-store D-07 absence-parity check proving the SQL CTE and InMemory loaders filter task status identically (this is the single-source-of-truth guard from 04-02 verified end-to-end).
    - The persistent subtree test asserts a terminal GOAL is present in the tree and a terminal TASK is absent (D-07 asymmetry proven on the SQL path).
    - The side-effect-free test asserts `service.events().len()` is identical before/after a persistent `period_view` call.
    - No raw item id is used as a comparison key in any parity assertion (keys are title/depth/kind).
  </acceptance_criteria>
  <done>Persistent subtree build, cross-store parity (D-11), and persistent side-effect-freedom are proven green; parity compares structure-capturing stable keys, and the D-07 visibility asymmetry holds identically on both stores.</done>
</task>

<task type="auto">
  <name>Task 2: SC3 store-level anomaly fixtures — cycle / orphan / over-depth terminate with anomaly_count, no Err</name>
  <read_first>
    - todo-engine/tests/integration/period_view.rs (the file being modified; reuse `persistent_service()` from Task 1 and `period_view`)
    - todo-engine/src/application/service/goal.rs (`validate_goal_nesting`:50 rejects cycles at create time — WHY the fixture must bypass the service API)
    - todo-engine/src/infrastructure/sqlite/repo.rs (the `INSERT INTO items (...)` column list :62-74 + `save_item_on` — model the raw row insert; or expose the connection via the test's own `connect`)
    - todo-engine/src/infrastructure/sqlite/schema.rs (the `items` table columns + `parent_id TEXT REFERENCES items(id)` :29 — note the FK; insert rows in an order that satisfies it, or build a 2-cycle by inserting both then UPDATE-ing parent_id, since a self/mutual cycle cannot satisfy a forward-only insert)
    - todo-engine/src/application/service/queries.rs (the `assemble` visited-set + `MAX_GOAL_DEPTH` behavior from Plan 01 — what anomaly_count counts)
    - todo-engine/src/application/service/mod.rs (`ServiceStore::InMemory(HashMap)` — for the hand-built in-memory back-edge map; note the store is `pub(super)` so the InMemory variant of this fixture may need to go through the persistent/raw-SQL path if the HashMap is not test-accessible — prefer the raw-SQLite injection as the primary fixture)
    - .planning/phases/04-period-view-goal-tree-rollup/04-RESEARCH.md (Wave 0 gap: anomaly fixture must be injected at the store level; SC3 test rows in the Test Map :389-394)
  </read_first>
  <action>
    Add SC3 anomaly tests that inject malformed `parent_id` data at the STORE level, bypassing `validate_goal_nesting`. Primary fixture path = raw SQLite: open the temp DB via `connect` + `init_schema` (as `persistent_service` does), insert goal rows directly with `conn.execute("INSERT INTO items (...) VALUES (...)", params![...])` using the real column list (mirror repo.rs:62-74; set the minimum NOT-NULL columns — `id`, `type`='goal', `title`, `status`='active', `proposed_by`, `created_at`, `updated_at`, `horizon`, `scheduled`, plus `parent_id`). Build the repository over that same connection (`SqliteTodoRepository::new`) and wrap in `TodoService::persistent`.

    Because `parent_id` has a forward FK (`REFERENCES items(id)`), construct a 2-node cycle by inserting goal A and goal B (each with `parent_id = NULL`), then `UPDATE items SET parent_id = ?` to point A→B and B→A (an UPDATE after both rows exist satisfies the FK). At least one of A/B must match the requested `(horizon, period_key)` so it is a CTE seed/root.

    Tests:
    - `cycle_is_severed_no_error` (SC3): with the A⇄B cycle injected, `period_view(Horizon::Month, "2026-06-01")` returns `Ok`, `anomaly_count >= 1`, and does not hang (the test completing IS the non-hang assertion; the SQL `UNION` bounds the load and the in-memory visited-set bounds the walk).
    - `orphan_parent_no_error` (SC3): insert a goal whose `parent_id` points to a non-existent id; `period_view` returns `Ok` and the orphan is handled (bumps anomaly_count per the Plan 01 orphan rule, or is simply unreachable — assert `Ok` + no panic, and document which the Plan 01 implementation does).
    - `depth_cap_truncates_persistent` (SC3): insert a chain deeper than `MAX_GOAL_DEPTH` (65+ goals linked head-to-tail by `parent_id`, root matching the period) and assert `period_view` returns `Ok` with `anomaly_count >= 1` and the returned tree depth does not exceed the cap. (Generate the chain in a loop; insert root first to satisfy the FK at each step.)

    For an InMemory-side anomaly check: if the `ServiceStore::InMemory` HashMap is not directly constructible from the test crate (it is `pub(super)`), document that the cycle/orphan SC3 guarantee is proven via the Persistent raw-SQL fixture (the `assemble` walk being shared code means the InMemory guard is the same code path) and add an in-memory assertion only if a public seeding path exists. Do NOT weaken the test to satisfy accessibility — prefer the raw-SQLite fixture which exercises the exact `assemble` guard.
  </action>
  <verify>
    <automated>cd todo-engine && cargo test --test integration period_view</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test integration period_view` green including `cycle_is_severed_no_error`, `orphan_parent_no_error`, `depth_cap_truncates_persistent`; the suite COMPLETES (no hang/timeout) — proving termination on cyclic data.
    - The cycle fixture is injected by raw SQL (insert two rows then UPDATE parent_id to form A⇄B), NOT via `propose_goal` — `grep -c 'INSERT INTO items' todo-engine/tests/integration/period_view.rs` returns ≥ 1 and the cycle test does not call `validate_goal_nesting`/`propose_goal` to create the cycle.
    - `cycle_is_severed_no_error` asserts `period_view(...)` is `Ok(_)` AND `view.anomaly_count >= 1`.
    - `depth_cap_truncates_persistent` builds a > `MAX_GOAL_DEPTH` (≥ 65) chain and asserts `Ok` + `anomaly_count >= 1` + bounded returned depth.
    - No test asserts a panic or an `Err` from `period_view` on malformed data (D-09: the view NEVER fails).
  </acceptance_criteria>
  <done>Store-level cyclic, orphaned, and over-depth fixtures prove SC3: `period_view` terminates and returns `Ok` with `anomaly_count` bumped on every store, never hanging or erroring; cycle data is injected at the SQLite row level, bypassing the create-time validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → temp SQLite (raw row injection) | Tests deliberately write malformed `parent_id` cycles/orphans the service would reject, to exercise the SC3 guard. Scoped entirely to per-test `tempfile` homes — never the live data home (CLAUDE.md: never aim destructive experiments at `~/.todo-engine/todo.sqlite`). |
| period_view → cyclic/orphaned working set | The system-under-test boundary: the loader + `assemble` must terminate on adversarial legacy data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Denial of Service | period_view on injected `parent_id` cycle / >depth chain | mitigate | This plan is the TEST that proves the mitigation: `cycle_is_severed_no_error` + `depth_cap_truncates_persistent` assert the call returns `Ok` with `anomaly_count` bumped and the suite terminates — verifying the SQL `UNION` + in-memory visited-set + `MAX_GOAL_DEPTH` cap from Plans 01/02. |
| T-04-08 | Tampering | test fixture targeting wrong DB | accept | All fixtures use `tempfile::tempdir()` per-test homes; no path touches the live data home; `*.sqlite` is gitignored. |

**No new exposure:** test-only plan; no production code, no new dependency (RESEARCH Package Legitimacy Audit: none added), no auth/mutation/PII change. No block-on-high finding.
</threat_model>

<verification>
- `cargo test --test integration period_view` green (all in-memory + persistent + parity + SC3 anomaly + side-effect-free tests).
- Suite terminates (no hang) on injected cycles — the SC3 non-hang proof.
- Parity asserted by structure-capturing stable keys; no raw-id comparisons.
- Cycle fixtures injected at the SQLite row level, bypassing create-time validation.
- Run `cargo test` (all three binaries) at wave merge to confirm e2e/integration stay green (CLAUDE.md layered-test invariant).
</verification>

<success_criteria>
- D-11/SC4/CORE-03: InMemory and Persistent produce identical trees for the same seed (parity test green).
- SC1/VIEW-03: Persistent SQL path builds the documented subtree over real SQLite, with the D-07 visibility asymmetry holding.
- SC3/D-08/D-09: cyclic / orphaned / over-depth legacy data terminates safely with `anomaly_count` bumped, `period_view` returns `Ok`, never hangs/Errs.
- SC3/CORE-03: persistent `period_view` is side-effect-free (no audit event).
</success_criteria>

<output>
Create `.planning/phases/04-period-view-goal-tree-rollup/04-03-SUMMARY.md` when done. Record: the store-level cycle-injection technique used (two inserts + UPDATE to form A⇄B), the final list of tests and which SC/requirement each locks, the observed `anomaly_count` semantics (what counts as one anomaly), and confirmation `cargo test` (all binaries) is green.
</output>
