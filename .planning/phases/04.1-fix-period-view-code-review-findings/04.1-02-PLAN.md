---
phase: 04.1-fix-period-view-code-review-findings
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - todo-engine/src/infrastructure/sqlite/repo.rs
  - todo-engine/src/application/ports.rs
autonomous: true
requirements: []   # advisory cleanup of Phase 4 — resolves review findings WR-01, WR-03; CONTEXT decisions D-01, D-02 (verified), D-03
user_setup: []

must_haves:
  truths:
    - "HARD INVARIANT: rendered PeriodView output (tree shape, node ordering, surfaced tasks) is UNCHANGED; Phase 4 SC1-SC4 stay green"
    - "The SQLite recursive CTE descends ONLY through goal parents — for goal G1(root) -> task T -> goal G2 the CTE working set is {G1, T} (G2 dropped), identical to the InMemory frontier walk"
    - "Direct-under-goal tasks still load (a task whose parent is a goal is pulled by the CTE)"
    - "D-02: rendered output is unchanged because assemble only descends goal->goal; G2 was never a rendered node"
    - "No new bound parameter is introduced; ?1/?2/?3.. binding order and status-placeholder generation are unchanged"
  artifacts:
    - path: "todo-engine/src/infrastructure/sqlite/repo.rs"
      provides: "Goal-only recursive descent in the WITH RECURSIVE CTE + D-11 clarifying comment"
      contains: "p.type = 'goal'"
    - path: "todo-engine/src/application/ports.rs"
      provides: "load_period_subtree doc whose 'same flat working set' / D-11 claim is now literally true"
  key_links:
    - from: "CTE recursive arm"
      to: "parent row type"
      via: "JOIN items p ON s.id = p.id AND p.type = 'goal' (tests the PARENT s.id, not child i)"
      pattern: "JOIN items p ON s\\.id = p\\.id AND p\\.type = 'goal'"
---

<objective>
Make the cross-store parity contract (D-11) true by construction: constrain the SQLite
recursive-CTE step in `repo.rs` so it descends only through *goal* parents, so the persistent
working set matches the InMemory frontier walk exactly.

- D-01 / WR-01: add `JOIN items p ON s.id = p.id AND p.type = 'goal'` to the recursive arm so a
  child is pulled only when its parent row is a goal. For `goal G1(root) -> task T -> goal G2`
  the CTE set becomes `{G1, T}` (G2 dropped — its parent is a task), identical to InMemory.
- D-03 / WR-03: add ONE clarifying comment at the new JOIN stating it enforces D-11; KEEP the
  existing "produce the SAME flat working set" doc wording in ports.rs/repo.rs (it becomes
  literally true). Do NOT take the Path-B doc-weakening route.
- D-02 (verify/state, no code): rendered output is unchanged — `assemble` only descends
  goal->goal via `goals_by_parent.get(&Some(goal.id))`, so G2 (parent = task T) was never a
  rendered node even with the old CTE.

Purpose: Today both stores happen to render the same tree only by an `assemble` implementation
detail; the documented "same flat working set" invariant is currently false on adversarial
`goal -> task -> goal` linkage. This plan makes the invariant enforced in SQL.

Output: `repo.rs` CTE with goal-parent descent + comment; `ports.rs` doc accurate. The D-07
cross-store parity fixture that enforces this lands in Plan 03 (Wave 2).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-fix-period-view-code-review-findings/04.1-CONTEXT.md
@.planning/phases/04.1-fix-period-view-code-review-findings/04.1-RESEARCH.md
@.planning/phases/04.1-fix-period-view-code-review-findings/04.1-PATTERNS.md
@.planning/phases/04-period-view-goal-tree-rollup/04-REVIEW.md
@todo-engine/src/infrastructure/sqlite/repo.rs
@todo-engine/src/application/ports.rs
@todo-engine/src/application/service/queries.rs
</context>

<artifacts_this_phase_produces>
NEW symbols introduced by THIS plan: none (the change is a static SQL predicate inside an
existing query string + a doc comment edit — no new Rust symbol).
MODIFIED in this plan: the recursive-CTE SQL string in `repo.rs`, and the doc comments on
`load_period_subtree` (ports.rs) / the repo parity doc block (repo.rs).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Constrain the recursive CTE to goal-parent descent (D-01) + D-11 comment (D-03)</name>
  <files>todo-engine/src/infrastructure/sqlite/repo.rs</files>
  <read_first>
    - todo-engine/src/infrastructure/sqlite/repo.rs — the current CTE (repo.rs:81-94), the
      status-placeholder generation (repo.rs:69-75), and the param push (repo.rs:98-103). Confirm
      the recursive arm is `SELECT i.id FROM items i JOIN subtree s ON i.parent_id = s.id WHERE
      i.type IN ('goal', 'task')` and that `subtree(id)` carries only `id`.
    - .planning/phases/04.1-fix-period-view-code-review-findings/04.1-PATTERNS.md §"repo.rs ... D-01, D-03"
      (lines 28-74) — the exact JOIN line to insert, the comment text, and Pitfall-3 (the JOIN must
      test the PARENT row `s.id`, NOT the child `i.id`).
    - .planning/phases/04.1-fix-period-view-code-review-findings/04.1-RESEARCH.md §"D-01 + D-03"
      (lines 68-138) — the node-by-node SQL trace proving the CTE set becomes {G1, T}, the
      "no new bound parameter" note, and the "keep UNION / keep WHERE i.type IN" notes.
    - .planning/phases/04-period-view-goal-tree-rollup/04-REVIEW.md §"WR-01" (lines 66-97) — the
      finding and the reviewer's suggested fix.
    - todo-engine/src/application/service/queries.rs:205-235 — the InMemory frontier walk this CTE
      must match (goals only ever seeded into `frontier`; tasks appended, never expanded).
  </read_first>
  <action>
    In the `WITH RECURSIVE subtree(id)` recursive arm, add one self-join immediately after
    `JOIN subtree s ON i.parent_id = s.id`:
    `JOIN items p ON s.id = p.id AND p.type = 'goal'`. Pitfall-3: the join key is `s.id = p.id`
    (the PARENT row already in the subtree), and the predicate is `p.type = 'goal'` — do NOT write
    `i.id = p.id` (that tests the child, the wrong row). Directly above the JOIN, add a single
    clarifying comment (D-03): the goal-parent JOIN exists so the CTE working set matches the
    InMemory frontier (goal->goal only); a goal whose parent is a task is unreachable in both
    stores, enforcing D-11. KEEP `WHERE i.type IN ('goal', 'task')` (direct-under-goal tasks must
    still load) and KEEP the deduplicating `UNION` (SQL-level cycle guard). Do NOT add or reorder
    any bound parameter — `?1`/`?2`/`?3..` and the `status_placeholders` generation/push stay
    exactly as-is (the added predicate is a static literal, no value interpolated, V5 unchanged).
    No fenced block here; the exact JOIN + comment text is in PATTERNS.md §repo.rs.
  </action>
  <verify>
    <automated>cd todo-engine && cargo test -p todo-engine --test integration period_view && cargo clippy --all-targets --all-features -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - The recursive CTE arm in repo.rs contains `JOIN items p ON s.id = p.id AND p.type = 'goal'`
      (parent-row test; NOT `i.id = p.id`).
    - A clarifying comment naming D-11 / goal-parent descent sits at the new JOIN.
    - `WHERE i.type IN ('goal', 'task')` and `UNION` (not `UNION ALL`) are unchanged; no new bound
      param appears (grep the file for `?` placeholders — count and order match pre-edit).
    - `cargo test -p todo-engine --test integration period_view` exits 0 (existing persistent CTE +
      parity assertions still pass; the new D-07 fixture lands in Plan 03); `cargo clippy ... -D warnings` exits 0.
  </acceptance_criteria>
  <done>CTE descends only through goal parents; direct-under-goal tasks still load; no new param; period_view suite + clippy green.</done>
</task>

<task type="auto">
  <name>Task 2: Make the parity docs accurate under D-01 (D-03 / WR-03)</name>
  <files>todo-engine/src/application/ports.rs, todo-engine/src/infrastructure/sqlite/repo.rs</files>
  <read_first>
    - todo-engine/src/application/ports.rs — the `TodoRepository::load_period_subtree` doc
      (ports.rs:11-23); the "produce identical tree shape (D-11)" / "same flat working set" claim
      at ports.rs:16-18.
    - todo-engine/src/infrastructure/sqlite/repo.rs:43-63 — the parity doc block ("the two stores
      cannot drift").
    - .planning/phases/04.1-fix-period-view-code-review-findings/04.1-PATTERNS.md §"ports.rs ... D-03"
      (lines 182-198) — KEEP the wording; optionally tighten to note goal-only descent now enforces
      it in code rather than by `assemble` coincidence.
    - .planning/phases/04-period-view-goal-tree-rollup/04-REVIEW.md §"WR-03" (lines 126-138) — the
      over-stated-doc finding.
  </read_first>
  <action>
    Doc-only edit. KEEP the existing "produce the SAME flat working set" / "the two stores cannot
    drift" / "identical tree shape (D-11)" wording in ports.rs:11-23 and repo.rs:43-63 — under
    Task 1 it is now literally true. Optionally tighten one sentence to state the goal-only CTE
    descent (Task 1's JOIN) is what enforces the identical flat working set, rather than relying on
    an `assemble` implementation detail. Do NOT weaken any claim to "identical rendered tree" (that
    is the rejected Path B). No structural change; no Rust symbol changes.
  </action>
  <verify>
    <automated>cd todo-engine && cargo build -p todo-engine && cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - ports.rs and repo.rs doc blocks retain the "same flat working set" / D-11 wording (not
      weakened to "rendered tree").
    - If tightened, the doc references the goal-parent CTE descent as the enforcement mechanism.
    - `cargo build -p todo-engine` exits 0; `cargo fmt --check` exits 0.
  </acceptance_criteria>
  <done>Parity docs are accurate (true by construction under D-01), wording not weakened; build + fmt clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite read (CTE) | The persistent loader reads rows via a `WITH RECURSIVE` CTE. `horizon` and `period_key` cross from the service into SQL — both already bound as `?1`/`?2`. This plan adds NO new value crossing this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-02 | Tampering (SQL injection) | `repo.rs` recursive CTE | accept (already mitigated) | The new `JOIN items p ON s.id = p.id AND p.type = 'goal'` is a STATIC SQL literal — no value is interpolated. `horizon`, `period_key`, and the status strings remain bound (`?1`/`?2`/`?3..`); status-placeholder count derives from `OPEN_STATUSES.len()` (an integer), never a value. 04-REVIEW headline confirmed the CTE clean. No new injection vector. |
| T-04.1-03 | Denial of Service (unbounded recursion) | `repo.rs` recursive CTE | accept | The deduplicating `UNION` (SQL-level cycle guard) is retained unchanged; the new JOIN narrows which children are pulled, never relaxing the guard. |
| T-04.1-SC | Tampering | npm/pip/cargo installs | n/a | No package install in this plan — no new dependency added. No `[ASSUMED]`/`[SUS]` package to gate. |

**Security delta: none.** The D-01 SQL edit is a constant predicate; no interpolation, no new endpoint, no surface change. No `<threat_model>` item rises to `high` (researcher: net security delta none).
</threat_model>

<verification>
- `cargo test -p todo-engine --test integration period_view` green (existing persistent CTE + parity assertions hold; D-07 fixture lands in Plan 03).
- `cargo build -p todo-engine` + `cargo fmt --check` + `cargo clippy --all-targets --all-features -- -D warnings` clean.
- Hard invariant: rendered `PeriodView` output unchanged — G2 was never a rendered node, so dropping it from the CTE working set is invisible to callers.
- No new bound parameter; status-placeholder generation untouched.
</verification>

<success_criteria>
- The recursive CTE descends only through goal parents (`JOIN items p ON s.id = p.id AND p.type = 'goal'`); for `goal -> task -> goal` the CTE set is `{G1, T}`, matching InMemory.
- Direct-under-goal tasks still load; rendered tree unchanged; Phase 4 SC1-SC4 green.
- Parity docs are accurate (D-11 true by construction), wording not weakened.
- `cargo fmt --check` and `cargo clippy --all-targets --all-features -- -D warnings` pass.
</success_criteria>

<output>
Create `.planning/phases/04.1-fix-period-view-code-review-findings/04.1-02-SUMMARY.md` when done.
</output>
