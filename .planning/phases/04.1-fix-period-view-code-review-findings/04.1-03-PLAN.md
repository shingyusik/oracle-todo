---
phase: 04.1-fix-period-view-code-review-findings
plan: 03
type: execute
wave: 2
depends_on:
  - 04.1-01
  - 04.1-02
files_modified:
  - todo-engine/src/application/service/mod.rs
  - todo-engine/tests/integration/period_view.rs
autonomous: true
requirements: []   # advisory cleanup of Phase 4 — resolves review findings WR-04, IN-03 + WR-02 regression coverage; CONTEXT decisions D-06, D-07, D-08
user_setup: []

must_haves:
  truths:
    - "HARD INVARIANT: rendered PeriodView output (tree shape, node ordering, surfaced tasks) is UNCHANGED; Phase 4 SC1-SC4 stay green"
    - "The test crate's MAX_GOAL_DEPTH is single-sourced from production via a pub use re-export — no hand-mirrored 64 remains"
    - "A goal(root) -> task -> goal cross-store fixture asserts tree_keys(mem) == tree_keys(disk) AND anomaly_count(mem) == anomaly_count(disk); G2 is absent and anomaly_count is 0 in BOTH stores"
    - "A valid D-02 sibling-root fixture (R2.parent_id = R1, both exact period matches) asserts anomaly_count == 0"
  artifacts:
    - path: "todo-engine/src/application/service/mod.rs"
      provides: "pub use goal::MAX_GOAL_DEPTH; — single source of truth for the depth cap on the public service path"
      contains: "pub use goal::MAX_GOAL_DEPTH"
    - path: "todo-engine/tests/integration/period_view.rs"
      provides: "insert_task_row raw helper + goal->task->goal cross-store parity fixture (D-07) + sibling-root anomaly==0 fixture (D-08); mirrored const deleted"
      contains: "insert_task_row"
  key_links:
    - from: "tests/integration/period_view.rs"
      to: "todo_engine::application::service::MAX_GOAL_DEPTH"
      via: "use import (replaces the deleted const)"
      pattern: "use todo_engine::application::service::\\{[^}]*MAX_GOAL_DEPTH"
    - from: "D-07 parity fixture"
      to: "raw goal->task->goal rows"
      via: "insert_goal_row + insert_task_row + insert_goal_row(parent=task) in a temp SQLite home"
      pattern: "insert_task_row"
---

<objective>
Add the test coverage the review flagged as missing and single-source the test depth cap —
locking the Plan 01 / Plan 02 production fixes against regression.

- D-06 / IN-03: add `pub use goal::MAX_GOAL_DEPTH;` to `service/mod.rs` (single source of truth
  on the public `service` path, respecting the `pub(super)` production const) and DELETE the
  hand-mirrored `const MAX_GOAL_DEPTH: usize = 64;` in `period_view.rs`, importing the re-export
  instead. RESEARCH A1: `pub use` of a `pub(super)` const must compile in Rust 2024 — the
  executor verifies with `cargo build`; fallback is a `#[cfg(test)]` `pub const` accessor.
- D-07 / WR-04: add a raw-injection cross-store parity fixture for `goal(root) -> task -> goal`
  (the shape WR-01's divergence lives in) and assert `tree_keys(mem) == tree_keys(disk)` AND
  `anomaly_count(mem) == anomaly_count(disk)`. Under Plan 02's D-01 fix both working sets are
  `{G1, T}` (G2 dropped), both render G1 with task T inlined, G2 absent in both, anomaly 0 in both.
- D-08: add a store-level fixture for a valid D-02 sibling-root config (R2.parent_id = R1, both
  exact period matches) and assert `anomaly_count == 0` — guards the Plan 01 D-04 over-count fix.

Purpose: The Phase 4 parity test never exercised `goal -> task -> goal`, so the one cross-store
divergence that existed could pass undetected; and no fixture pinned the sibling-root
anomaly==0 behavior. This plan makes both contracts enforced rather than assumed.

Output: `service/mod.rs` re-export; `period_view.rs` with `insert_task_row`, the D-07 parity
fixture, the D-08 sibling-root fixture, and the mirrored const deleted.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-fix-period-view-code-review-findings/04.1-CONTEXT.md
@.planning/phases/04.1-fix-period-view-code-review-findings/04.1-RESEARCH.md
@.planning/phases/04.1-fix-period-view-code-review-findings/04.1-PATTERNS.md
@.planning/phases/04-period-view-goal-tree-rollup/04-REVIEW.md
@todo-engine/src/application/service/mod.rs
@todo-engine/src/application/service/goal.rs
@todo-engine/tests/integration/period_view.rs
</context>

<artifacts_this_phase_produces>
NEW symbols introduced by THIS plan (exclude from any source-drift / "symbol does not exist" verification):
- `pub use goal::MAX_GOAL_DEPTH;` in `service/mod.rs` — re-export making `todo_engine::application::service::MAX_GOAL_DEPTH` resolvable (the production const itself, `pub(super) const MAX_GOAL_DEPTH` in goal.rs:11, already exists and is NOT new).
- `fn insert_task_row(conn, id, title, scheduled: Option<&str>, parent_id: Option<&str>)` in `period_view.rs` — raw task-insert test helper, mirror of the existing `insert_goal_row`.
- The two new test fns in `period_view.rs`: the `goal->task->goal` cross-store parity fixture (D-07) and the sibling-root `anomaly_count == 0` fixture (D-08). The executor names them descriptively (e.g. `parity_goal_task_goal_cross_store`, `sibling_root_nesting_is_not_an_anomaly`).

DELETED in this plan: the hand-mirrored `const MAX_GOAL_DEPTH: usize = 64;` at period_view.rs:631-635.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Single-source MAX_GOAL_DEPTH via pub use re-export (D-06 / IN-03)</name>
  <files>todo-engine/src/application/service/mod.rs, todo-engine/tests/integration/period_view.rs</files>
  <read_first>
    - todo-engine/src/application/service/mod.rs — the existing `pub use` re-export block
      (mod.rs:18-22, where `pub use queries::{GoalNode, PeriodView};` sits) and `mod goal;`
      (private, mod.rs:12).
    - todo-engine/src/application/service/goal.rs:11 — `pub(super) const MAX_GOAL_DEPTH: usize = 64;`
      (the production source of truth).
    - todo-engine/tests/integration/period_view.rs:631-635 — the hand-mirrored `const
      MAX_GOAL_DEPTH: usize = 64;` to delete, and period_view.rs:1-5 — the existing
      `use todo_engine::application::service::{...}` import to extend.
    - .planning/phases/04.1-fix-period-view-code-review-findings/04.1-PATTERNS.md §"service/mod.rs ... D-06"
      (lines 202-226) and §"D-06: consume the single-sourced const" (lines 234-243) — the exact
      `pub use goal::MAX_GOAL_DEPTH;` line and the visibility-convention note.
    - .planning/phases/04.1-fix-period-view-code-review-findings/04.1-RESEARCH.md §"D-06 + IN-03"
      (lines 235-265) and the A1 assumption (line 506) — verify the `pub use` of a `pub(super)`
      const compiles; fallback is a `#[cfg(test)]` `pub const` accessor.
    - .planning/phases/04-period-view-goal-tree-rollup/04-REVIEW.md §"IN-03" (lines 177-186).
  </read_first>
  <action>
    In `service/mod.rs`, add ONE line alongside the existing `pub use` block (mirroring
    `pub use queries::{GoalNode, PeriodView};`): `pub use goal::MAX_GOAL_DEPTH;`. Keep the
    production const `pub(super)` in goal.rs — only the re-export widens it on the public
    `service` path (respects the `pub(super)` convention; single source of truth). In
    `period_view.rs`, DELETE the hand-mirrored `const MAX_GOAL_DEPTH: usize = 64;` and its
    sync-acknowledging comment (period_view.rs:631-635), and add `MAX_GOAL_DEPTH` to the existing
    `use todo_engine::application::service::{...}` import at the top of the file. The
    `depth_cap_truncates_persistent` test (period_view.rs:594-629) then asserts against the
    re-exported cap unchanged. RESEARCH A1: run `cargo build` to confirm the `pub use` of a
    `pub(super)` const compiles; if it does NOT, fall back to a `#[cfg(test)]`-gated `pub const`
    accessor in `service` (still single-source — do not re-introduce a hand-mirrored test const).
    No fenced block here; the exact lines are in PATTERNS.md §D-06.
  </action>
  <verify>
    <automated>cd todo-engine && cargo build -p todo-engine && cargo test -p todo-engine --test integration period_view && cargo clippy --all-targets --all-features -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `service/mod.rs` contains `pub use goal::MAX_GOAL_DEPTH;` (or, fallback, a `#[cfg(test)]`
      `pub const` accessor — NOT a hand-mirrored test const).
    - `period_view.rs` no longer declares `const MAX_GOAL_DEPTH` and imports it from
      `todo_engine::application::service`.
    - `cargo build -p todo-engine` exits 0 (confirms A1 visibility widening compiles);
      `cargo test -p todo-engine --test integration period_view` exits 0 (`depth_cap_truncates_persistent`
      passes against the single-sourced cap); `cargo clippy ... -D warnings` exits 0.
  </acceptance_criteria>
  <done>MAX_GOAL_DEPTH has one source; the test imports the re-export; depth fixture green; build + clippy clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: insert_task_row helper + goal->task->goal cross-store parity fixture (D-07 / WR-04)</name>
  <files>todo-engine/tests/integration/period_view.rs</files>
  <read_first>
    - todo-engine/tests/integration/period_view.rs — the existing parity test
      `parity_in_memory_vs_persistent` (period_view.rs:391-425) to mirror its assert shape; the raw
      helpers `raw_home()` (period_view.rs:460-466), `insert_goal_row` (period_view.rs:474-497),
      `service_over` (period_view.rs:500-502); `tree_keys` (period_view.rs:70-89); and the FK
      handling note around the orphan fixture (period_view.rs:557-575).
    - .planning/phases/04.1-fix-period-view-code-review-findings/04.1-PATTERNS.md §"D-07 ... (WR-04)"
      (lines 245-293) — the `insert_task_row` column contract (type='task', open status 'active',
      no horizon column needed), the parent-first insert order (Pitfall-4), Pitfall-5 (open status
      so T stays visible), and the in-memory-seeding OPEN QUESTION to resolve below.
    - .planning/phases/04.1-fix-period-view-code-review-findings/04.1-RESEARCH.md §"D-07" (lines
      267-311) + Open Question 1 (lines 512-522) + Assumption A3 (line 508) — the recommended
      loader-level comparison.
    - todo-engine/src/application/service/goal.rs (validate_goal_nesting) — confirms the validating
      service API rejects goal-under-task, which is WHY raw injection is required.
    - .planning/phases/04-period-view-goal-tree-rollup/04-REVIEW.md §"WR-04" (lines 140-154).
  </read_first>
  <behavior>
    - Under Plan 02's D-01 CTE fix, the persistent `period_view` over `goal G1(root) -> task T ->
      goal G2` yields working set `{G1, T}`: G1 rendered, task T inlined under G1, G2 ABSENT,
      `anomaly_count == 0`.
    - The fixture asserts `tree_keys(persistent) == tree_keys(in_memory_equivalent)` AND
      `anomaly_count(persistent) == anomaly_count(in_memory_equivalent)` — both equal, G2 absent in both.
    - If Plan 02's fix were absent/wrong, G2 would leak into the persistent tree and the assert
      would FAIL (this is the WR-01 divergence the test must catch).
  </behavior>
  <action>
    Add an `insert_task_row(conn, id, title, scheduled: Option&lt;&str&gt;, parent_id: Option&lt;&str&gt;)`
    raw helper mirroring `insert_goal_row`'s column contract but with `type = 'task'` and an OPEN
    `status = 'active'` (Pitfall-5: a terminal status would be filtered out); tasks need no
    `horizon` column for the CTE. Build the persistent half by raw-injecting PARENT-FIRST into a
    `raw_home()` (Pitfall-4 / FK): `insert_goal_row(conn, "G1", "g1-root", "month", "2026-06-01", None)`,
    then `insert_task_row(conn, "T", ..., Some("G1"))`, then `insert_goal_row(conn, "G2",
    "g2-under-task", "week", "2026-06-08", Some("T"))`; wrap with `service_over` and call
    `period_view(Horizon::Month, "2026-06-01")`.

    RESOLVE RESEARCH Open Question 1 (in-memory seeding) using the recommended loader-level
    comparison (option b), refined so it respects the `pub(super)` ServiceStore boundary: the
    InMemory store is not raw-constructible and the validating API rejects goal-under-task, so seed
    the in-memory comparison side with the rendered-EQUIVALENT valid shape that the API DOES permit
    — `goal G1(root)` with a linked open task T under it (G2 simply not created, because under D-01
    it is provably unreachable in the persistent store too). Assert the persistent
    `goal->task->goal` view's `tree_keys` and `anomaly_count` EQUAL the in-memory `goal->task`
    view's `tree_keys` and `anomaly_count`. This proves the exact D-01 contract — G2 does not leak
    into the persistent tree — without adding a production test-only store accessor. Compare
    `tree_keys` (title, depth, kind), NEVER raw ids (in-memory uses `goal_000001`, persistent uses
    UUIDs). Do NOT widen the production surface (no `#[cfg(test)]` InMemory seeding hook). No fenced
    block here; the helper contract and fixture steps are in PATTERNS.md §D-07.
  </action>
  <verify>
    <automated>cd todo-engine && cargo test -p todo-engine --test integration period_view && cargo clippy --all-targets --all-features -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - period_view.rs defines `insert_task_row` (type='task', status='active') and a new test fn that
      raw-injects `goal->task->goal` into a persistent service.
    - The new test asserts persistent `tree_keys` == in-memory-equivalent `tree_keys` AND persistent
      `anomaly_count` == in-memory-equivalent `anomaly_count`; G2 is absent from the persistent tree
      keys; both `anomaly_count` values are 0.
    - No production test-only InMemory store accessor is added (the `pub(super)` ServiceStore
      boundary is unchanged).
    - `cargo test -p todo-engine --test integration period_view` exits 0; `cargo clippy ... -D warnings` exits 0.
  </acceptance_criteria>
  <done>insert_task_row exists; the goal->task->goal cross-store fixture proves G2 does not leak and anomaly parity holds; suite + clippy green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Valid sibling-root anomaly_count == 0 regression fixture (D-08 / WR-02 regression)</name>
  <files>todo-engine/tests/integration/period_view.rs</files>
  <read_first>
    - todo-engine/tests/integration/period_view.rs — the existing raw fixtures cycle/orphan
      (period_view.rs:511-543, 553-587) as the structure analog (`raw_home` + `insert_goal_row` +
      `service_over` + a single `period_view` assertion); `seed_goal_tree` (period_view.rs:103-122)
      to confirm why a NEW raw fixture is needed (its siblings are service-validated and not
      parent/child).
    - .planning/phases/04.1-fix-period-view-code-review-findings/04.1-PATTERNS.md §"D-08 ..."
      (lines 295-312) — the R1/R2 construction (insert R1 then R2, FK satisfied, no PRAGMA), and the
      before/after reasoning (pre-fix R2 bumps the count; post-fix root_ids.contains short-circuits to 0).
    - .planning/phases/04.1-fix-period-view-code-review-findings/04.1-RESEARCH.md §"D-08" (lines
      313-325) — store-level coverage is sufficient (cross-store NOT required; D-07 covers that).
    - .planning/phases/04-period-view-goal-tree-rollup/04-REVIEW.md §"WR-02" (lines 99-124) — the
      over-count finding this fixture regression-guards.
  </read_first>
  <behavior>
    - Raw-inject root R1 at `(month, 2026-06-01)` with `parent_id = None` and root R2 at the SAME
      `(month, 2026-06-01)` with `parent_id = Some("R1")` — both exact period matches → both roots by D-02.
    - `period_view(Horizon::Month, "2026-06-01").anomaly_count == 0` after the Plan 01 D-04 fix.
    - Before the D-04 fix this would assert >= 1 (R2 fails `visited.insert` in R1's child bucket) —
      the fixture is the regression guard.
  </behavior>
  <action>
    Add a store-level (NOT cross-store) test fn following the cycle/orphan fixture shape: build a
    `raw_home()`, `insert_goal_row(&home.conn, "R1", "root-1", "month", "2026-06-01", None)` then
    `insert_goal_row(&home.conn, "R2", "root-2", "month", "2026-06-01", Some("R1"))` (insert R1
    before R2 so the FK is satisfied directly — no `PRAGMA foreign_keys` toggling needed), wrap with
    `service_over`, call `period_view(Horizon::Month, "2026-06-01").unwrap()`, and
    `assert_eq!(view.anomaly_count, 0)` with a comment noting D-04: a valid sibling-root must not
    over-count. No fenced block here; the construction is in PATTERNS.md §D-08.
  </action>
  <verify>
    <automated>cd todo-engine && cargo test -p todo-engine --test integration period_view && cargo clippy --all-targets --all-features -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - period_view.rs defines a new test fn that raw-injects R1 (parent None) and R2 (parent R1),
      both `(month, 2026-06-01)`, and asserts `view.anomaly_count == 0`.
    - The fixture uses parent-first inserts (R1 then R2) with no `PRAGMA foreign_keys` toggle.
    - `cargo test -p todo-engine --test integration period_view` exits 0 (the new fixture is green
      against the Plan 01 D-04 fix); `cargo clippy ... -D warnings` exits 0.
  </acceptance_criteria>
  <done>Sibling-root fixture asserts anomaly_count == 0 and is green; regression guard for the D-04 over-count fix in place; suite + clippy clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-only raw SQLite injection | The D-07/D-08 fixtures intentionally bypass `TodoService` via raw `INSERT`s into a TEMP SQLite home to model legacy/corrupt rows the validating API rejects. This is test code only — never the production write path; the live data home is never touched (`raw_home()` is a tempfile). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-04 | Tampering | raw-injection test fixtures bypassing service validation | accept | This is the established, documented adversarial-fixture pattern (period_view.rs:439-466). Raw rows are required because `validate_goal_nesting` rejects `goal->task->goal` and same-period parent/child at create time. Scope is a tempfile SQLite home; no production mutation path is added or bypassed. |
| T-04.1-05 | Elevation of Privilege (visibility widening) | `pub use goal::MAX_GOAL_DEPTH;` | accept | The re-export widens ONLY the single `MAX_GOAL_DEPTH` const on the public `service` path (a non-sensitive integer cap); the production const stays `pub(super)`. No broad surface widening; respects the `pub(super)` convention. |
| T-04.1-SC | Tampering | npm/pip/cargo installs | n/a | No package install in this plan — no new dependency added (`rusqlite`/`tempfile` already in dev-deps). No `[ASSUMED]`/`[SUS]` package to gate. |

**Security delta: none.** No external surface, no input parsing, no new interpolation. No `<threat_model>` item rises to `high`.
</threat_model>

<verification>
- `cargo test -p todo-engine --test integration period_view` green: depth fixture passes against the single-sourced cap; D-07 parity fixture proves G2 does not leak (persistent == in-memory-equivalent); D-08 fixture asserts sibling-root anomaly_count == 0.
- `cargo build -p todo-engine` confirms the `pub use` visibility widening compiles (A1).
- `cargo test` (full workspace) + `cargo fmt --check` + `cargo clippy --all-targets --all-features -- -D warnings` clean.
- Hard invariant: rendered `PeriodView` output unchanged — these are test-only and re-export-only changes; no production tree logic is touched.
</verification>

<success_criteria>
- `MAX_GOAL_DEPTH` is single-sourced via `pub use goal::MAX_GOAL_DEPTH;`; the hand-mirrored test const is deleted; the test imports the re-export.
- The `goal->task->goal` cross-store fixture asserts tree_keys + anomaly_count equality and proves G2 absent in both (D-01 enforced, not assumed).
- The valid sibling-root fixture asserts anomaly_count == 0 (D-04 regression guarded).
- Phase 4 SC1-SC4 stay green; rendered `PeriodView` output unchanged.
- `cargo fmt --check` and `cargo clippy --all-targets --all-features -- -D warnings` pass.
</success_criteria>

<output>
Create `.planning/phases/04.1-fix-period-view-code-review-findings/04.1-03-SUMMARY.md` when done.
</output>
