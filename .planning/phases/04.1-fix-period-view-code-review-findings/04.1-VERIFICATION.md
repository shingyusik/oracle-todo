---
phase: 04.1-fix-period-view-code-review-findings
verified: 2026-06-25T07:10:00Z
status: passed
score: 11/11 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 04.1: Fix Period-View Code-Review Findings Verification Report

**Phase Goal:** Resolve all 04-REVIEW.md findings so the cross-store parity contract is true rather than coincidental — make the SQL CTE and in-memory walk produce the same flat working set (Path A / D-01 — goal-only descent), stop anomaly_count over-counting valid D-02 sibling-root nesting, add a goal→task→goal parity fixture that exercises the divergence, plus the minor cleanups (HashSet root lookup, shared sort comparator, single-sourced test depth cap). No change to rendered PeriodView output.
**Verified:** 2026-06-25T07:10:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

The phase resolves all 7 Phase-4 review findings (WR-01 through WR-04, IN-01 through IN-03). Each was verified against the actual codebase, not SUMMARY claims. All three production/test contracts are present, substantive, wired, and behaviorally proven by a green targeted test suite.

### Observable Truths

| # | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1 | HARD INVARIANT: rendered PeriodView output unchanged; Phase 4 SC1-SC4 stay green | ✓ VERIFIED | period_view 15/15 green incl. `parity_in_memory_vs_persistent`, `roots_are_exact_period_matches`, `node_ordering_is_deterministic`, `descendants_cross_period_included`. All production changes are counter-only / working-set-narrowing; no tree-build logic altered. |
| 2 | WR-01/D-01: recursive CTE descends ONLY through goal parents; goal→task→goal working set is {G1,T} (G2 dropped), matching InMemory frontier | ✓ VERIFIED | repo.rs:99 `JOIN items p ON s.id = p.id AND p.type = 'goal'` (parent-row test, not child); D-11 comment repo.rs:96-98. Proven by `parity_goal_task_goal_cross_store` asserting G2 absent. |
| 3 | Direct-under-goal tasks still load | ✓ VERIFIED | repo.rs:100 `WHERE i.type IN ('goal', 'task')` retained; fixture asserts `t-under-goal` present in disk keys (period_view.rs:494-498). |
| 4 | WR-02/D-04: valid D-02 sibling-root config yields anomaly_count == 0 (no over-count) | ✓ VERIFIED | queries.rs:376-381 `if root_ids.contains(&child.id) { continue; }` placed FIRST in child loop, before visited/depth guard. `sibling_root_nesting_is_not_an_anomaly` asserts anomaly_count == 0 AND both surface as roots. |
| 5 | Genuine cycle / over-depth / orphan re-visits still bump anomaly_count (no under-count) | ✓ VERIFIED | queries.rs:382-386 visited/depth guard unchanged, sits after the root short-circuit. `depth_cap_truncates` + `depth_cap_truncates_persistent` assert anomaly_count >= 1 and depth bounded; both green. |
| 6 | IN-01: root_ids is a HashSet (kills O(roots×goals) scan) | ✓ VERIFIED | queries.rs:296 `let mut root_ids: HashSet<String> = HashSet::new();`, populated via `.insert()` (line 303); membership filter `root_ids.contains` now O(1) (line 321). |
| 7 | IN-02/D-05: sort_date_view and sort_child_goals delegate to one shared comparator (byte-identical ordering) | ✓ VERIFIED | queries.rs:251 single `fn schedule_then_created_order`; sort_date_view (264) and sort_child_goals (408) both single `sort_by(schedule_then_created_order)` delegations. No duplicated closure. date_view 4/4 + period_view ordering green. |
| 8 | WR-03/D-03: parity docs accurate (not weakened to "rendered tree"), cite goal-only CTE descent as enforcement | ✓ VERIFIED | ports.rs:17-21 retains "identical tree shape (D-11)" + "identical to the InMemory frontier walk by construction"; repo.rs:51-56 D-01 goal-parent descent block. No Path-B weakening. |
| 9 | WR-04/D-07: goal→task→goal cross-store fixture asserts tree_keys + anomaly_count parity; G2 absent both stores | ✓ VERIFIED | `parity_goal_task_goal_cross_store` (period_view.rs:444-499) raw-injects G1→T→G2, asserts tree_keys(disk)==tree_keys(mem), anomaly_count==0 both, and G2 not in disk keys. Green. |
| 10 | IN-03/D-06: test MAX_GOAL_DEPTH single-sourced from production via re-export; no hand-mirrored 64 | ✓ VERIFIED | mod.rs:37 `pub const MAX_GOAL_DEPTH: usize = goal::MAX_GOAL_DEPTH;`; period_view.rs:2 imports it; no `const MAX_GOAL_DEPTH: usize = 64` anywhere in tests (grep empty). |
| 11 | No new bound parameter introduced; ?1/?2/?3.. binding order + status-placeholder generation unchanged | ✓ VERIFIED | repo.rs:79-114 — added JOIN predicate is a static literal `p.type = 'goal'`; status_placeholders still derived from OPEN_STATUSES.len(); param push order unchanged. |

**Score:** 11/11 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `todo-engine/src/application/service/queries.rs` | HashSet root_ids, root-aware anomaly skip, shared comparator | ✓ VERIFIED | All three present and wired into assemble/build_node; contains `schedule_then_created_order`, `root_ids.contains`, HashSet. |
| `todo-engine/src/infrastructure/sqlite/repo.rs` | Goal-only CTE descent + D-11 comment | ✓ VERIFIED | Contains `JOIN items p ON s.id = p.id AND p.type = 'goal'`; comment present; WHERE/UNION/params unchanged. |
| `todo-engine/src/application/ports.rs` | Parity doc true under D-01 | ✓ VERIFIED | "identical tree shape (D-11)" retained + goal-parent enforcement note. |
| `todo-engine/src/application/service/mod.rs` | pub const MAX_GOAL_DEPTH single source | ✓ VERIFIED | mod.rs:37 accessor binding (A1 fallback to `pub const` — `pub use` of pub(super) is E0364 in Rust 2024). |
| `todo-engine/tests/integration/period_view.rs` | insert_task_row + D-07 + D-08 fixtures; mirrored const deleted | ✓ VERIFIED | `insert_task_row` (579), `parity_goal_task_goal_cross_store` (444), `sibling_root_nesting_is_not_an_anomaly` (714); hand-mirrored const removed. |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| build_node child loop | root_ids HashSet | `root_ids.contains(&child.id) continue` FIRST in loop | ✓ WIRED | queries.rs:376-381, precedes visited.insert guard (Pitfall-2 honored). |
| sort_date_view / sort_child_goals | schedule_then_created_order | `sort_by(schedule_then_created_order)` | ✓ WIRED | queries.rs:265, 409. |
| CTE recursive arm | parent row type | `JOIN items p ON s.id = p.id AND p.type='goal'` | ✓ WIRED | repo.rs:99 (tests parent s.id, not child i). |
| period_view.rs | service::MAX_GOAL_DEPTH | use import replaces deleted const | ✓ WIRED | period_view.rs:2 import; used at lines 778-779. |
| D-07 fixture | raw goal→task→goal rows | insert_goal_row + insert_task_row + insert_goal_row(parent=task) | ✓ WIRED | period_view.rs:448-463. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Period-view suite incl. both new fixtures | `cargo test --test integration period_view` | 15 passed; 0 failed | ✓ PASS |
| Date-view ordering preserved by comparator extraction | `cargo test --test integration date_view` | 4 passed; 0 failed | ✓ PASS |
| Sibling-root anomaly == 0 | `sibling_root_nesting_is_not_an_anomaly` | ok | ✓ PASS |
| G2 does not leak (goal→task→goal) | `parity_goal_task_goal_cross_store` | ok | ✓ PASS |
| Genuine over-depth still counts | `depth_cap_truncates_persistent` | ok (anomaly_count >= 1) | ✓ PASS |
| No hand-mirrored test const | grep `const MAX_GOAL_DEPTH: usize = 64` in tests/ | no matches | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| (none new) | 01/02/03 (`requirements: []`) | Advisory cleanup of Phase 4 review findings; VIEW-03/VIEW-04 already satisfied (`[x]`) | ✓ N/A | All three plans declare empty requirements. No orphaned IDs map to phase 04.1 in REQUIREMENTS.md. Nothing to reconcile. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (none) | — | — | — | No TODO/FIXME/XXX/TBD/HACK/PLACEHOLDER/unimplemented! in queries.rs or repo.rs. No stubs; all changes are complete in-place edits. |

### Human Verification Required

None. All must-haves are programmatically verifiable (SQL predicate, in-memory walk logic, fixture assertions). No visual/UX/real-time/external-service dimension — this is a backend tree-walk + SQL-CTE phase with full test coverage. The HARD INVARIANT (rendered output unchanged) is enforced by the green parity/ordering fixtures rather than requiring manual inspection.

### Pre-Existing Failure (Out of Scope — Not a Phase-04.1 Gap)

`cli::init_loads_todo_engine_home_from_dotenv` (e2e) fails identically at the phase base commit a786e2e — proven pre-existing, logged in deferred-items.md and STATE.md. It lives entirely in the CLI/dotenv/data-home path, untouched by this phase (which edits only queries.rs, repo.rs, ports.rs, mod.rs, and the period_view test file). Correctly NOT treated as a regression.

### Gaps Summary

None. All 7 review findings (WR-01..WR-04, IN-01..IN-03) and their CONTEXT decisions (D-01, D-03, D-04, D-05, D-06, D-07, D-08) are resolved in the codebase: the goal-only CTE descent and the matching in-memory frontier produce identical flat working sets by construction; the root-aware anomaly short-circuit fixes the D-02 over-count while preserving genuine anomaly counting; the comparator is single-sourced; root_ids is a HashSet; the test depth cap is single-sourced; and two new fixtures lock the divergence and the over-count fix against regression. The HARD INVARIANT (no change to rendered PeriodView output) holds, confirmed by the full period_view + date_view suites staying green. One auto-fixed deviation (the `cycle_is_severed_no_error` fixture re-asserting anomaly_count == 0) is correct: that fixture modeled two same-period roots, which is exactly the D-02 sibling-root case the phase's authoritative must-have requires to be 0.

---

_Verified: 2026-06-25T07:10:00Z_
_Verifier: Claude (gsd-verifier)_
