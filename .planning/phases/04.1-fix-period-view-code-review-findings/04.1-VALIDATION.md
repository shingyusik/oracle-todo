---
phase: 04.1
slug: fix-period-view-code-review-findings
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-25
---

# Phase 04.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in test harness (`cargo test`) |
| **Config file** | none — workspace `Cargo.toml`; layered suites under `todo-engine/tests/{unit,integration,e2e}` |
| **Quick run command** | `cargo test -p todo-engine --test integration period_view` |
| **Full suite command** | `cargo test` |
| **Estimated runtime** | ~30–90 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p todo-engine --test integration period_view`
- **After every plan wave:** Run `cargo test`
- **Before `/gsd-verify-work`:** Full suite must be green, plus `cargo fmt --check` and `cargo clippy --all-targets --all-features -- -D warnings`
- **Max feedback latency:** ~90 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| {N}-01-01 | 01 | 1 | None new (advisory) | — | N/A — pure refactor, no behavior change | integration | `cargo test -p todo-engine --test integration period_view` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*
*Populated from PLAN.md task IDs after planning; the top validation concern is the hard invariant — rendered `PeriodView` output (tree shape, node ordering, surfaced tasks) is UNCHANGED and Phase 4 SC1–SC4 stay green.*

---

## Wave 0 Requirements

- [ ] `todo-engine/tests/integration/period_view.rs` — add `goal→task→goal` cross-store parity fixture (D-07) + valid sibling-root anomaly fixture (D-08)
- [ ] raw-insert test helper(s) for `goal→task→goal` seeding (research Open Question 1 — in-memory seeding mechanism decided in planning)

*Existing `cargo test` infrastructure covers all phase requirements; no framework install needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| — | — | — | — |

*All phase behaviors have automated verification (integration parity assertions + cargo fmt/clippy gates).*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 90s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
