# Phase 04.1: Fix period-view code review findings - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-25
**Phase:** 04.1-fix-period-view-code-review-findings
**Areas discussed:** Parity contract (WR-01/03), Anomaly over-count (WR-02), Minor cleanups (IN-01/02/03), Test coverage (WR-04)

---

## Area selection

| Option | Description | Selected |
|--------|-------------|----------|
| Parity 계약 (WR-01/03) | CTE goal-only descent vs. doc weakening | ✓ |
| Anomaly over-count (WR-02) | root_ids threaded into build_node | ✓ |
| Minor cleanups (IN-01/02/03) | HashSet / shared comparator / single-source depth cap | ✓ |
| 테스트 커버리지 (WR-04) | goal→task→goal parity fixture + assertions | ✓ |

**User's choice:** All four areas selected for deep-dive.

---

## Parity contract (WR-01 / WR-03)

| Option | Description | Selected |
|--------|-------------|----------|
| CTE goal-only descent (Path A) | Add goal-parent JOIN to CTE recursive step → both loaders identical flat working set; D-11 enforced in code; G2 dropped from CTE set; rendered tree unchanged | ✓ |
| Weaken docs to rendered-tree (Path B) | Leave CTE; reword ports.rs/repo.rs doc to "identical rendered tree", cite assemble goal-only descent | |
| Both (CTE fix + doc clarification) | Path A plus rewriting doc to describe the actual mechanism | |

**User's choice:** Path A — CTE goal-only descent (recommended).
**Notes:** Keep existing "same flat working set" doc (now literally true) + add one CTE comment explaining the goal-parent JOIN guards D-11. Aligns with the project's "enforce invariants in code" philosophy.

---

## Anomaly over-count (WR-02)

| Option | Description | Selected |
|--------|-------------|----------|
| root_ids HashSet threaded into build_node | Make root_ids a HashSet<String>, thread into build_node; root children continue without bump → resolves WR-02 + IN-01 together | ✓ |
| Thread root_ids but keep Vec | Same logic, keep Vec; decide IN-01 separately | |

**User's choice:** root_ids HashSet threaded into build_node (recommended).
**Notes:** Folds IN-01 (O(n²)→O(n)) into the WR-02 fix.

---

## Minor cleanups (IN-02 / IN-03)

| Option | Description | Selected |
|--------|-------------|----------|
| IN-02 sort comparator extraction | Extract schedule_then_created_order helper; both sorts delegate | ✓ |
| IN-03 test depth cap single-sourced | Expose MAX_GOAL_DEPTH to test crate via test-only accessor | ✓ |

**User's choice:** Both IN-02 and IN-03.
**Notes:** IN-01 already handled by the WR-02 decision. IN-03's "leave as-is" fallback was not taken — single-source preferred.

---

## Test coverage (WR-04)

| Option | Description | Selected |
|--------|-------------|----------|
| anomaly_count equality assert | WR-04 fixture asserts tree_keys AND anomaly_count equality across stores | ✓ |
| WR-02 D-02 sibling-root regression | Valid sibling-root fixture asserting anomaly_count == 0 | ✓ |
| All new fixtures cross-store | Run both new fixtures on InMemory + Persistent | |

**User's choice:** anomaly_count equality assert + WR-02 sibling-root regression.
**Notes:** WR-04 parity fixture is cross-store by nature; the WR-02 regression is store-level (cross-store not required). The blanket "all fixtures cross-store" option was not selected.

---

## Claude's Discretion

- Exact SQL phrasing of the goal-parent JOIN (D-01) and the precise mechanism for the test-only `MAX_GOAL_DEPTH` accessor (D-06), as long as the invariant and `pub(super)` visibility convention hold.

## Deferred Ideas

None — discussion stayed within phase scope (advisory cleanup of Phase 4 review findings).
