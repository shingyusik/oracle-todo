---
phase: 05-cli-api-surface-parity-locked
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - todo-engine/tests/e2e/cli.rs
  - todo-engine/tests/e2e/api.rs
autonomous: true
requirements: [SURF-01, SURF-02]
must_haves:
  truths:
    - "Paired e2e tests prove the CLI and API surfaces yield the SAME item state for goal-create, task-link, and all three views (SC3 — state parity)."
    - "Paired e2e tests prove the CLI and API surfaces yield the SAME rejection for present-but-invalid input (bad horizon / junk date / non-Goal parent) — exit 2 / HTTP 400 with matching detail (SC3 — rejection parity, Pitfall 3)."
    - "Tests prove an agent-created goal via EITHER surface starts `proposed` and cannot be created `active` directly (SC4 — approval gating not bypassable)."
    - "An API task→goal link test asserts the returned item's `parent_id` is the goal id and NOT null (Pitfall 1 regression guard for the handlers.rs:212 fix)."
  artifacts:
    - path: "todo-engine/tests/e2e/cli.rs"
      provides: "CLI e2e tests: goal propose JSON+proposed, agenda/date-range/period JSON, update --parent-id links, present-but-invalid rejection (exit 2)"
      contains: "goal"
    - path: "todo-engine/tests/e2e/api.rs"
      provides: "API e2e tests: POST /goals/propose, GET /views/*, PATCH /items/:id parent_id non-null, present-but-invalid rejection (400)"
      contains: "/goals/propose"
  key_links:
    - from: "tests/e2e/cli.rs"
      to: "Command::cargo_bin(\"todo-engine\") goal/agenda/date-range/period/update"
      via: "assert_cmd over a TestHome, parse JSON stdout"
      pattern: "cargo_bin"
    - from: "tests/e2e/api.rs"
      to: "router(&db_path) /goals/propose, /views/*, PATCH /items/:id"
      via: "oneshot via json_request/empty_request, parse body_json"
      pattern: "oneshot"
---

<objective>
Add paired CLI + API e2e tests that PROVE the two Phase 5 surfaces are in parity: a goal proposed via the CLI and via `POST /goals/propose` yield the same item state and start `proposed` (SC4); the three views return the same JSON shape on both surfaces; task→goal linking works on both; and present-but-invalid inputs reject identically (exit 2 / HTTP 400). This is the Nyquist Dimension-8 contract for the phase — every SURF requirement gets an automated e2e assertion.

Purpose: Satisfy SC3 (paired e2e tests asserting same item state AND same rejections) and SC4 (approval gating not bypassable from the API), closing SURF-01/SURF-02 with proof rather than assertion. Per RESEARCH §Validation Architecture, the phase→test map is: SURF-01 → CLI e2e, SURF-02 → API e2e, SC3 → paired e2e, SC4 → `status == "proposed"` on both surfaces.
Output: New `#[test]`/`#[tokio::test]` fns appended to the two existing e2e files; `cargo test --test e2e` green for the new tests (and the full suite green modulo the documented pre-existing dotenv failure).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cli-api-surface-parity-locked/05-CONTEXT.md
@.planning/phases/05-cli-api-surface-parity-locked/05-RESEARCH.md
@.planning/phases/05-cli-api-surface-parity-locked/05-PATTERNS.md
@.planning/phases/05-cli-api-surface-parity-locked/05-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan adds test functions only (no production symbols). New `#[test]` fns in `tests/e2e/cli.rs` and `#[tokio::test]` fns in `tests/e2e/api.rs`:

CLI tests (`tests/e2e/cli.rs`):
- `goal_propose_prints_proposed_json` — `goal propose` ⇒ JSON `"type":"goal"`, `"status":"proposed"`, `"proposed_by":"agent"` (SC4)
- `agenda_date_range_period_emit_json` — all three views print parseable JSON (not Markdown)
- `update_parent_id_links_task_to_goal` — `update <t> --parent-id <g>` ⇒ JSON `parent_id == <g>`
- `period_bad_horizon_exits_two` — `period --horizon bogus ..` ⇒ exit 2 (present-but-invalid rejection)

API tests (`tests/e2e/api.rs`):
- `goal_propose_returns_proposed_item` — `POST /goals/propose` ⇒ 200, `"status":"proposed"`, `"proposed_by":"agent"` (SC4)
- `view_routes_return_json` — `GET /views/{agenda,date-range,period}` ⇒ 200 + JSON of expected shape
- `patch_item_parent_id_links_and_is_not_null` — `PATCH /items/:id {parent_id}` ⇒ 200, `item["parent_id"]` equals goal id (Pitfall-1 regression)
- `view_period_bad_horizon_returns_400` — `GET /views/period?horizon=bogus..` ⇒ 400 with `{"detail": ..}`

Optional helper (if used): a small comparison assertion shared inline — independent assertion in each file matches the current idiom and is acceptable per VALIDATION.md Wave 0.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: CLI e2e — goal propose, three views, update --parent-id, bad-horizon rejection (SURF-01, SC4, SC3-CLI side)</name>
  <read_first>
    - todo-engine/tests/e2e/cli.rs — `task_propose_prints_json_item` (`:76-100`): `TestHome::new()` → `init` → run command → `.assert().success().stdout(contains("\"status\":\"proposed\""))`; uses `--home <path>` + `assert_cmd::Command::cargo_bin("todo-engine")` and `predicates::str::contains`
    - todo-engine/tests/e2e.rs — module wiring (`#[path = "e2e/cli.rs"] mod cli;`, `#[path = "support/mod.rs"] mod support;`)
    - todo-engine/tests/support/mod.rs — `TestHome` (`new()`, `path()`, `db_path()`)
    - .planning/phases/05-cli-api-surface-parity-locked/05-PATTERNS.md §8 — CLI test analog + parity/rejection assertion rules (Pitfall 3: assert byte/`detail`-identical rejection ONLY for present-but-invalid input; for missing positional args assert only the class)
    - .planning/phases/05-cli-api-surface-parity-locked/05-RESEARCH.md — Pitfall 3 (missing-param rejection differs across surfaces; use present-but-invalid for strict-parity rejection tests)
  </read_first>
  <action>
    Append `#[test]` fns to `tests/e2e/cli.rs`, each over a fresh `TestHome::new()` with an `init` first (mirror `task_propose_prints_json_item`):
    (1) `goal_propose_prints_proposed_json`: run `goal propose "<title>" --horizon month --scheduled 2026-06-01`; assert `.success()` and stdout `contains("\"type\":\"goal\"")`, `contains("\"status\":\"proposed\"")`, `contains("\"proposed_by\":\"agent\"")` (SC4).
    (2) `agenda_date_range_period_emit_json`: run `agenda 2026-06-26`, `date-range 2026-06-01 2026-06-30`, and `period --horizon month --period 2026-06-01`; for each assert `.success()` and that stdout parses as JSON — assert a JSON-structural substring is present (e.g. `period` stdout `contains("\"period_key\"")` and `contains("\"roots\"")`; agenda/date-range stdout starts with `[`). Do NOT assert Markdown table markers (D-01 — JSON only).
    (3) `update_parent_id_links_task_to_goal`: propose a goal and a task (capture their ids from stdout JSON via `serde_json::from_slice` on the captured output bytes — use `.output()` / `assert().get_output()`), then run `update <task-id> --parent-id <goal-id> --scheduled 2026-06-29`; assert stdout JSON `parent_id` equals the goal id and `scheduled` equals `2026-06-29`.
    (4) `period_bad_horizon_exits_two`: run `period --horizon bogus --period 2026-06-01`; assert `.failure()` with `.code(2)` (present-but-invalid ⇒ `TodoError::Validation` ⇒ exit 2). This is the CLI half of the SC3 rejection-parity pair (the API half is the 400 test in Task 2).
    Use the existing `--home <path>` + `Command::cargo_bin("todo-engine")` idiom; reuse `predicates::str::contains`. Scope assertions to the new behavior — do not touch the pre-existing dotenv test.
  </action>
  <verify>
    <automated>cargo test --test e2e cli && cargo clippy --all-targets --all-features -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/cli.rs` contains the four `#[test]` fns named above (or equivalent), each over a `TestHome`.
    - `cargo test --test e2e cli` runs them and they pass (exit 0).
    - `goal_propose_prints_proposed_json` asserts `"status":"proposed"` and `"proposed_by":"agent"` (SC4).
    - `period_bad_horizon_exits_two` asserts exit code 2 for a present-but-invalid horizon (SC3 CLI rejection).
    - `cargo clippy --all-targets --all-features -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>Every new CLI command has an automated e2e test proving JSON output, goal approval-gating default (SC4), task linking, and present-but-invalid rejection (exit 2).</done>
</task>

<task type="auto">
  <name>Task 2: API e2e — POST /goals/propose, GET /views/*, PATCH parent_id non-null, bad-horizon 400 (SURF-02, SC4, SC3-API side + parity)</name>
  <read_first>
    - todo-engine/tests/e2e/api.rs — helpers `body_json` (`:7`), `json_request` (`:12`), `empty_request` (`:21`), `http_request` (`:29`); `task_propose_and_items_use_same_service_path` (`:66-104`): `router(&db_path)` over a `tempfile::tempdir()` `todo.sqlite`, `oneshot` POST, assert `status()==200`, parse body, assert `item["status"]=="proposed"`, `item["proposed_by"]=="agent"`; `router(":memory:")` idiom (`:48`)
    - todo-engine/src/interfaces/api/mod.rs — `router(db_path)` signature (accepts a file path or `:memory:`)
    - .planning/phases/05-cli-api-surface-parity-locked/05-PATTERNS.md §8 — API test analog; state parity = assert independently in each file against the same service path (matches current idiom, no shared cross-surface helper); rejection parity only for present-but-invalid; Pitfall 1 = assert `item["parent_id"]` is the goal id and NOT null
    - .planning/phases/05-cli-api-surface-parity-locked/05-RESEARCH.md — Pitfall 1 (parent_id non-null), Pitfall 3 (use present-but-invalid `?horizon=bogus` for the strict 400 parity; a missing `?date=` is a `QueryRejection` whose body differs from clap — assert only class there)
  </read_first>
  <action>
    Append `#[tokio::test]` fns to `tests/e2e/api.rs`, each building `router(&db_path)` over a `tempfile::tempdir()` `todo.sqlite` (mirror `task_propose_and_items_use_same_service_path`; use a fresh `router(&db_path)` per request, as the existing test does, since each call opens its own connection over the same file):
    (1) `goal_propose_returns_proposed_item`: `json_request(app, "POST", "/goals/propose", json!({"title":"Q3 OKR","horizon":"month","scheduled":"2026-06-01"}))`; assert `status()==200`, then `body_json` ⇒ `item["type"]=="goal"`, `item["status"]=="proposed"`, `item["proposed_by"]=="agent"` (SC4 — mirrors the CLI assertion ⇒ state parity).
    (2) `view_routes_return_json`: `empty_request(app, "GET", "/views/agenda?date=2026-06-26")`, `.../views/date-range?from=2026-06-01&to=2026-06-30`, and `.../views/period?horizon=month&period=2026-06-01`; for each assert `status()==200` and parse `body_json`; assert the agenda/date-range bodies are JSON arrays and the period body has keys `period_key` and `roots` (same shape the CLI emits ⇒ view parity).
    (3) `patch_item_parent_id_links_and_is_not_null` (Pitfall-1 regression): POST `/goals/propose` to get a goal id, POST `/tasks/propose` to get a task id, then `json_request(app, "PATCH", format!("/items/{task_id}"), json!({"parent_id": goal_id, "scheduled":"2026-06-29"}))`; assert `status()==200` and `body_json` ⇒ `item["parent_id"] == goal_id` (NOT null) and `item["scheduled"]=="2026-06-29"`. Explicitly assert non-null to guard the `handlers.rs:212` fix.
    (4) `view_period_bad_horizon_returns_400`: `empty_request(app, "GET", "/views/period?horizon=bogus&period=2026-06-01")`; assert `status()==400` and `body_json["detail"]` is present (present-but-invalid ⇒ `TodoError::Validation` ⇒ 400 ⇒ pairs with the CLI exit-2 test ⇒ SC3 rejection parity). Optionally also assert that a goal created via `POST /goals/propose` cannot be created `active` (no body field sets status; the returned status is `proposed`) to reinforce SC4 non-bypass.
    Reuse the existing `json_request`/`empty_request`/`body_json` helpers; scope assertions to the new behavior.
  </action>
  <verify>
    <automated>cargo test --test e2e api && cargo clippy --all-targets --all-features -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/api.rs` contains the four `#[tokio::test]` fns named above (or equivalent), each over a `router(&db_path)`.
    - `cargo test --test e2e api` runs them and they pass (exit 0).
    - `goal_propose_returns_proposed_item` asserts 200 + `item["status"]=="proposed"` + `item["proposed_by"]=="agent"` (SC4 + state parity with the CLI test in Task 1).
    - `patch_item_parent_id_links_and_is_not_null` asserts `item["parent_id"]` equals the goal id and is NOT null (Pitfall-1 regression guard).
    - `view_period_bad_horizon_returns_400` asserts HTTP 400 with a `detail` body (pairs with the CLI exit-2 test ⇒ SC3 rejection parity).
    - `cargo clippy --all-targets --all-features -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>Every new API endpoint has an automated e2e test; goal-create state parity (SC4) and present-but-invalid rejection parity (SC3) with the CLI are proven, and the Pitfall-1 non-null `parent_id` regression is locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → CLI process / axum router | Tests drive untrusted-input shapes (valid + present-but-invalid) across both surfaces to prove the validation/gating boundaries hold. No new runtime trust boundary is introduced — these are test assertions over the surfaces added in 05-01/05-02. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-TEST-01 | Elevation of Privilege (proof) | approval-gating bypass (SC4) | mitigate | Tests assert an agent-created goal on BOTH surfaces starts `proposed` and that no surface path creates it `active` directly — converting the inherited service guarantee into an enforced regression test. |
| T-05-TEST-02 | Tampering (proof) | input-validation parity (SC3) | mitigate | Tests assert present-but-invalid input (bad horizon) rejects as exit 2 / HTTP 400 with matching `detail` on both surfaces, locking the `TodoError::Validation` mapping against regression. |
| T-05-TEST-03 | Tampering (proof) | API link no-op regression (Pitfall 1) | mitigate | `patch_item_parent_id_links_and_is_not_null` asserts non-null `parent_id`, permanently guarding the `handlers.rs:212` de-hardcode so a future refactor cannot silently revert it. |
| T-05-TEST-SC | Tampering | package installs | accept | No new packages — all test crates (`assert_cmd`, `predicates`, `tower`, `http`, `http_body_util`, `tempfile`, `serde_json`) are already dev-dependencies used by the existing e2e files. No Package Legitimacy Audit required. |
</threat_model>

<verification>
- `cargo test --test e2e` exits 0 for the new tests (the per-task quick gate per VALIDATION.md sampling).
- After this Wave-2 merge, run the FULL suite `cargo test` — all three binaries (unit/integration/e2e) green, scoped against the one documented pre-existing red dotenv test (`init_loads_todo_engine_home_from_dotenv`, out of scope per RESEARCH §Environment Availability; do NOT fix it here).
- `cargo clippy --all-targets --all-features -- -D warnings` and `cargo fmt --check` pass (run `cargo fmt` if needed before commit).
- Phase gate before `/gsd-verify-work`: full `cargo test` green (modulo the documented dotenv failure) + clippy + fmt.
</verification>

<success_criteria>
- Every new CLI command and API endpoint has a paired automated e2e test (SC3).
- Goal-create state parity and present-but-invalid rejection parity across CLI and API are proven (SC3).
- Agent-created goals start `proposed` on both surfaces; approval gating is not bypassable from the API (SC4).
- API `parent_id` is asserted non-null (Pitfall-1 regression locked).
- `cargo test --test e2e` green; full suite green modulo the documented dotenv failure; clippy + fmt clean.
</success_criteria>

<output>
Create `.planning/phases/05-cli-api-surface-parity-locked/05-03-SUMMARY.md` when done.
</output>
