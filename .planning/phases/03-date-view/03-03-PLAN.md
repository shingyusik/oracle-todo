---
phase: 03-date-view
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - todo-engine/tests/integration/date_view.rs
  - todo-engine/tests/integration.rs
autonomous: true
requirements: [VIEW-02, VIEW-05]
must_haves:
  truths:
    - "agenda/date_range return identical results over a persistent SQLite store as over in-memory — store parity (SC4), proven by parity_in_memory_vs_persistent running ONE fixture through both stores and asserting equal ordered sequences compared by stable key (title+scheduled, NOT raw ids)"
    - "Calling agenda/date_range writes no audit event — service.events().len() is unchanged before/after, proving no routine materialization (SC4/CORE-03)"
    - "The persistent path honors the same open-only + union + scheduled-range + unscheduled semantics as the unit suite (VIEW-02/VIEW-05 over SQLite)"
  artifacts:
    - path: "todo-engine/tests/integration/date_view.rs"
      provides: "Persistent SQLite parity + side-effect-free integration tests for SC4"
      min_lines: 40
    - path: "todo-engine/tests/integration.rs"
      provides: "#[path] registration of the date_view integration module"
      contains: "mod date_view"
  key_links:
    - from: "tests/integration/date_view.rs"
      to: "TodoService::persistent over a tempfile SQLite home"
      via: "persistent_service() idiom mirrored from goal_view.rs"
      pattern: "TodoService::persistent"
    - from: "tests/integration/date_view.rs"
      to: "service.events().len()"
      via: "before/after capture proving side-effect-free (SC4)"
      pattern: "events\\(\\)\\.len\\(\\)"
    - from: "tests/integration/date_view.rs"
      to: "TodoService::in_memory + TodoService::persistent"
      via: "one fixture run through both stores, compared by stable key (SC4 parity)"
      pattern: "TodoService::in_memory"
---

<objective>
Create the persistent SQLite integration test suite `tests/integration/date_view.rs` and register it in the `integration` test binary. This proves SC4: identical results across `InMemory` and persistent SQLite stores (CLI/API parity foundation, CORE-03) and side-effect-free behavior (calling the view writes no audit event — no routine materialization).

Purpose: SC4 / CORE-03 require the date-view methods to return identical results regardless of caller and to be pure reads. The persistent-store test mirrors the established `goal_view.rs` idiom. Tests are first-class deliverables per VALIDATION.md.
Output: `tests/integration/date_view.rs` (new) and one `#[path]` line in `tests/integration.rs` registering it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-date-view/03-CONTEXT.md
@.planning/phases/03-date-view/03-VALIDATION.md
@.planning/phases/03-date-view/03-PATTERNS.md
@.planning/phases/03-date-view/03-01-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
New symbols/files created in this plan:

- `todo-engine/tests/integration/date_view.rs` — new integration test module with a `persistent_service()` helper (mirrored from `goal_view.rs`) and `#[test]` fns proving SC4 store parity and side-effect-free behavior. Test fns: `persistent_agenda_unions_scheduled_and_due_open_tasks`, `persistent_date_range_groups_by_scheduled`, `agenda_is_side_effect_free`, `parity_in_memory_vs_persistent` (the explicit cross-store parity oracle), plus a `seed_fixture(&mut service)` helper shared by the parity test
- One new `#[path = "integration/date_view.rs"] mod date_view;` registration line in `todo-engine/tests/integration.rs`

(`agenda`/`date_range` and their helpers are created in plan 03-01.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write tests/integration/date_view.rs for SC4 store parity + side-effect-free</name>
  <files>todo-engine/tests/integration/date_view.rs</files>
  <read_first>
    - todo-engine/tests/integration/goal_view.rs — the EXACT idiom to mirror: `persistent_service()` setup (lines 20-27, copy verbatim), imports (lines 1-4, adapt `ProposeGoal` → `ProposeTask`/`UpdateItem`), and the id-collection assertion idiom (lines 32-93)
    - .planning/phases/03-date-view/03-01-SUMMARY.md — the real final signatures of `agenda`/`date_range` to call against
    - todo-engine/src/application/service/mod.rs — `events()` returns `&[TodoEvent]` (line 57); `service.events().len()` is the side-effect-free oracle; `TodoService::persistent` (line 47) and `TodoService::in_memory` (line 37) for the parity comparison
    - todo-engine/src/application/service/creation.rs — `ProposeTask` fields (lines 12-22) for building the fixture
    - .planning/phases/03-date-view/03-VALIDATION.md — § "Key oracles / assertions": side-effect-free (events().len() unchanged), store parity (compare by a stable key, NOT raw ids — in-memory uses seeded ids, persistent uses UUIDs)
  </read_first>
  <action>
    Create `tests/integration/date_view.rs`. Copy the `persistent_service()` helper verbatim from `goal_view.rs` (lines 20-27): `tempfile::tempdir()` → `connect(db_path)` → `init_schema` → `SqliteTodoRepository::new` → `TodoService::persistent`. Adapt the imports to bring in `ProposeTask`, `UpdateItem`, `TodoService` from `todo_engine::application::service`, `ListFilter` from `todo_engine::application::ports`, `Actor` from `todo_engine::domain`, and the SQLite trio from `todo_engine::infrastructure::sqlite`. Use `tempfile::tempdir()` directly (do NOT use `tests/support::TestHome` — it is only registered in `e2e.rs`).

    Add a shared fixture helper `fn seed_fixture(service: &mut TodoService)` that, given any `&mut TodoService` (in-memory OR persistent), creates the SAME set of open tasks via the real service API (`propose_task` with `Actor::User`, then `approve`/`activate`, then `update_item` to set `scheduled`/`due`). Give every task a DISTINCT, deterministic `title` (e.g. `"sched-in-range"`, `"due-only"`, `"both-match"`, `"unscheduled"`) so a stable key (title+scheduled) can identify rows independent of the store's id scheme. This helper is what makes the parity test run "the SAME fixture" through both stores.

    Implement these `#[test]` fns:

    1. `persistent_agenda_unions_scheduled_and_due_open_tasks` (SC4 over persistent store, re-proves SC3/VIEW-05): stand up a persistent service; create open tasks (propose with `Actor::User`, approve/activate, set `scheduled`/`due` via `update_item`) covering the union — one `scheduled == D`, one `due == D`, one both; call `service.agenda("2026-06-23")`; assert union membership and that the both-match task appears once. This confirms the persistent `list_items` path yields the same set the unit suite proves in-memory.
    2. `persistent_date_range_groups_by_scheduled` (SC4 / VIEW-02 over persistent store): create open tasks with scheduled dates inside and outside a range plus one unscheduled; call `service.date_range("2026-06-01", "2026-06-30")`; assert only the in-range scheduled tasks are returned, in deterministic order.
    3. `agenda_is_side_effect_free` (SC4 / CORE-03 — the core oracle): on the persistent service, capture `let before = service.events().len();`, call `service.agenda("2026-06-23")` (and optionally `date_range`), then assert `service.events().len() == before`. An unchanged event count proves NO routine materialization wrote an audit event — the view is a pure read.
    4. `parity_in_memory_vs_persistent` (SC4 store parity — the explicit cross-store oracle required by VALIDATION.md "identical results regardless of caller"): build `let mut mem = TodoService::in_memory();` and `let (_dir, mut disk) = persistent_service();`, call `seed_fixture(&mut mem)` and `seed_fixture(&mut disk)` so the IDENTICAL fixture is loaded into both stores. Run the SAME query through both — for the range: `let mem_rows = mem.date_range("2026-06-01", "2026-06-30")?;` and `let disk_rows = disk.date_range("2026-06-01", "2026-06-30")?;`; and for the agenda: `mem.agenda("2026-06-23")?` vs `disk.agenda("2026-06-23")?`. Map each result Vec to an ordered `Vec<(String, Option<String>)>` of the STABLE KEY = `(title, scheduled)` (NOT raw ids — in-memory uses seeded `task_000001` ids, persistent uses UUIDs, per VALIDATION.md store-parity note), and `assert_eq!(mem_keys, disk_keys)` for BOTH the range and the agenda query. Equal ordered key sequences prove the two stores return identical results in identical order.

    Store-parity note (VALIDATION.md): in-memory ids are deterministic seeds (`task_000001`); persistent ids are UUIDs. Do NOT compare raw ids across stores. The cross-store comparison in `parity_in_memory_vs_persistent` MUST use the title+scheduled stable key (or another store-independent ordering invariant), never raw id equality. For tests that stay entirely within the persistent service, asserting on the ids that service produced is fine.

    Mirror `goal_view.rs`'s assertion style: collect ids/titles into a `Vec` and `assert_eq!` against the expected ordered sequence.
  </action>
  <verify>
    <automated>cd todo-engine && cargo test --test integration date_view 2>&1 | grep -v '^#' | grep -Eq 'test result: ok|0 failed' && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'fn persistent_agenda_unions_scheduled_and_due_open_tasks' todo-engine/tests/integration/date_view.rs` returns 1; same for `persistent_date_range_groups_by_scheduled`, `agenda_is_side_effect_free`, and `parity_in_memory_vs_persistent`
    - Source assertion: `grep -c 'TodoService::persistent' todo-engine/tests/integration/date_view.rs` returns ≥ 1, `grep -c 'TodoService::in_memory' todo-engine/tests/integration/date_view.rs` returns ≥ 1, and `grep -c 'events().len()' todo-engine/tests/integration/date_view.rs` returns ≥ 1
    - Behavior assertion (SC4 side-effect-free): `service.events().len()` is equal before and after the `agenda` call
    - Behavior assertion (SC4 parity): `parity_in_memory_vs_persistent` runs the identical `seed_fixture` through both `in_memory` and `persistent` services and asserts the ordered `(title, scheduled)` key sequences are equal for BOTH `agenda` and `date_range` (NOT compared by raw id)
    - Test command: `cd todo-engine && cargo test --test integration date_view::parity_in_memory_vs_persistent` passes, and `cd todo-engine && cargo test --test integration date_view` passes (all four tests green)
  </acceptance_criteria>
  <done>`tests/integration/date_view.rs` proves persistent-store parity (including an explicit cross-store fixture comparison by stable key) and side-effect-free behavior; the integration command is green.</done>
</task>

<task type="auto">
  <name>Task 2: Register the date_view module in the integration test binary</name>
  <files>todo-engine/tests/integration.rs</files>
  <read_first>
    - todo-engine/tests/integration.rs — the existing `#[path]` registration lines (1-16); `date_view` sorts before `events` (line 1), so it becomes the first entry
    - .planning/phases/03-date-view/03-PATTERNS.md — § `tests/integration.rs` (the exact registration line and placement)
  </read_first>
  <action>
    Insert the registration for the new integration module into `tests/integration.rs`. `date_view` sorts alphabetically before `events`, so add the `#[path = "integration/date_view.rs"]` attribute and `mod date_view;` as the first entry (before the existing `events` registration), matching the surrounding style exactly. No other change to the file.
  </action>
  <verify>
    <automated>cd todo-engine && cargo test --test integration date_view 2>&1 | grep -v '^#' | grep -Eq 'test result: ok' && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'mod date_view' todo-engine/tests/integration.rs` returns 1 and the preceding line contains `#[path = "integration/date_view.rs"]`
    - Test command: `cd todo-engine && cargo test --test integration date_view` discovers and runs the date_view tests (non-zero test count, all ok)
    - Full suite: `cd todo-engine && cargo test` passes; `cd todo-engine && cargo fmt --check` passes; `cd todo-engine && cargo clippy --all-targets --all-features -- -D warnings` passes
  </acceptance_criteria>
  <done>The `date_view` integration module is registered and discovered; the full suite, fmt, and clippy gates are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test code only. The persistent SQLite store lives in a throwaway `tempfile::tempdir()` home — never the live `~/.todo-engine/todo.sqlite` (CLAUDE.md data-home safety). No runtime input surface, no auth boundary added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | persistent test fixture | accept | Tests target a `tempfile::tempdir()` SQLite home, isolated from the live data home (CLAUDE.md gotcha: never aim destructive experiments at the canonical DB). Mirrors the existing `goal_view.rs` safe idiom. |
| T-03-SC | Tampering | cargo installs | n/a | No package installs — uses built-in `#[test]` and existing dev deps (`tempfile` 3.15, `rusqlite`). No Package Legitimacy Audit required. |

**Honest assessment:** Test-only plan over an isolated temp SQLite home. No high or medium threats. The phase adds no network, auth, crypto, or SQL-construction surface (the view composes `list_items`); HTTP API is Phase 5.
</threat_model>

<verification>
- `cd todo-engine && cargo test --test integration date_view` runs all tests green
- `cd todo-engine && cargo test` (full suite) passes
- `cd todo-engine && cargo fmt --check` passes
- `cd todo-engine && cargo clippy --all-targets --all-features -- -D warnings` passes
- `date_view` registered in `tests/integration.rs`
</verification>

<success_criteria>
- SC4 store parity: persistent SQLite agenda/range honor the same semantics as the in-memory unit suite (VIEW-02/VIEW-05 over the persistent path), AND `parity_in_memory_vs_persistent` explicitly asserts identical ordered results (by stable key) across both stores for the same fixture
- SC4 side-effect-free: `service.events().len()` unchanged before/after `agenda`/`date_range` (no materialization audit write)
- CORE-03: the view logic is shared service code proven identical regardless of caller
</success_criteria>

<output>
Create `.planning/phases/03-date-view/03-03-SUMMARY.md` when done. Record the test names (including `parity_in_memory_vs_persistent`), the `seed_fixture` helper, and the `persistent_service` helper.
</output>
</output>
