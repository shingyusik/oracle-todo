---
milestone: "1.0"
name: Planning Layer
audited: 2026-06-26T03:45:00Z
status: passed
re_audited_after_merge: "feature/workspace-item-table-editing (merge e00bb3b)"
scores:
  requirements: 17/17
  phases: 6/6
  integration: clean (0 blockers, 1 non-regression warning)
  flows: complete
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: "01-domain-schema-foundation"
    items:
      - "No VALIDATION.md — Nyquist coverage not recorded (phase VERIFICATION passed; unit tests green)"
  - phase: "02-service-policy-goal-create-link-validation"
    items:
      - "Nyquist PARTIAL (nyquist_compliant: false) — formal Dimension validation not fully recorded"
      - "SUMMARY frontmatter bookkeeping: LINK-01/LINK-02/VIEW-01 not listed per-plan (confirmed SATISFIED by 02-VERIFICATION.md)"
  - phase: "03-date-view"
    items:
      - "Nyquist PARTIAL (nyquist_compliant: false)"
  - phase: "04.1-fix-period-view-code-review-findings"
    items:
      - "Nyquist PARTIAL (nyquist_compliant: false)"
  - phase: "05-cli-api-surface-parity-locked"
    items:
      - "Code review WR-01: API approve/complete handlers accept no body (reason=None hardcoded) while CLI accepts --reason. Behaviorally inert today (service binds _reason unused); latent silent-drop, outside SURF scope. See 05-REVIEW.md."
  - phase: "post-merge (feature/workspace-item-table-editing)"
    items:
      - "Surface asymmetry (non-regression): CLI `list` hardcodes parent_id/horizon/scheduled filters to None while API /items exposes all three. CORE-03 still satisfied (both share service.list_items + apply_list_filter); only the adapter exposure differs. Pre-existing, not introduced by the merge. Optional follow-up: expose the flags on CLI ListArgs."
nyquist:
  compliant_phases: ["04", "05"]
  partial_phases: ["02", "03", "04.1"]
  missing_phases: ["01"]
  overall: partial
deferred_v2:
  - "COVER-01: coverage view (goals with zero linked tasks)"
  - "ROLL-01: progress rollup (completion %/counts up the goal tree)"
  - "ROLL-02: health/at-risk derived signals"
  - "KR-01: numeric Key Results / metric targets on goals"
  - "CARRY-01: period rollover / carry-forward of incomplete tasks"
---

# Milestone v1.0 — Planning Layer — Audit

**Status:** ✅ passed
**Audited:** 2026-06-26

## Definition of Done

Add a planning layer to `todo-engine`: hierarchical period goals (year/month/week) that
decompose top-down into tasks, plus date-based and goal-tree views — all through the same
policy-enforced engine (validation, status state machine, audit events, approval gating),
with CLI and HTTP API kept at parity over a single `TodoService`.

## Requirements Coverage — 17/17 satisfied

3-source cross-reference (REQUIREMENTS.md traceability × phase VERIFICATION.md × SUMMARY frontmatter)
plus a runtime integration check. Every v1 requirement is WIRED end-to-end.

| REQ | Phase | Status | Evidence |
|-----|-------|--------|----------|
| GOAL-01 | 2 | ✅ satisfied | `propose_goal` via CLI `goal propose` / API `POST /goals/propose` |
| GOAL-02 | 1 | ✅ satisfied | `normalize_to_period_start` → `validate_goal_anchor` → `item.scheduled` |
| GOAL-03 | 2 | ✅ satisfied | `validate_goal_anchor` strict reject (no auto-snap) |
| GOAL-04 | 2 | ✅ satisfied | `validate_goal_nesting` (cycle + inverted-horizon reject) |
| GOAL-05 | 2 | ✅ satisfied | `ensure_goal_not_duplicate` on `(horizon, scheduled, parent_id)` |
| LINK-01 | 2 | ✅ satisfied | `update_item` parent_id guard; SC4 + e2e link tests |
| LINK-02 | 2 | ✅ satisfied | `update_item` scheduled set; positive test asserts scheduled |
| VIEW-01 | 2 | ✅ satisfied | `ListFilter` horizon/parent/period, both backends |
| VIEW-02 | 3 | ✅ satisfied | `agenda` + `date_range`, unscheduled bucket surfaced |
| VIEW-03 | 4 | ✅ satisfied | `period_view` goal-tree, dual-store parity proven |
| VIEW-04 | 4 | ✅ satisfied | period_view surfaces open unscheduled tasks under goal |
| VIEW-05 | 3 | ✅ satisfied | `agenda` scheduled ∪ due union |
| SURF-01 | 5 | ✅ satisfied | CLI subcommands, JSON output |
| SURF-02 | 5 | ✅ satisfied | API mirror over TodoService, paired e2e parity |
| CORE-01 | 2 | ✅ satisfied | all mutations via `store_item_and_event`, no direct repo writes |
| CORE-02 | 1 | ✅ satisfied | additive schema (Goal enum + indexes, no `period_key`) |
| CORE-03 | 5 | ✅ satisfied | view logic in service layer; CLI/API call identical methods |

No orphaned requirements. No unsatisfied requirements. The 5 v2 requirements
(COVER-01, ROLL-01, ROLL-02, KR-01, CARRY-01) are explicitly deferred and out of scope.

## Phases — 6/6 verified

| Phase | Name | VERIFICATION |
|-------|------|--------------|
| 1 | Domain + Schema Foundation | ✅ passed |
| 2 | Service Policy — Goal Create/Link/Validation | ✅ passed |
| 3 | Date View | ✅ passed |
| 4 | Period View (goal-tree rollup) | ✅ passed |
| 04.1 | Fix period-view review findings | ✅ passed |
| 5 | CLI + API Surface (parity-locked) | ✅ passed |

## Cross-Phase Integration — clean

Integration checker (gsd-integration-checker): **0 blockers, 0 warnings.**
- Phase 1 `Horizon`/`normalize_to_period_start` actually consumed by Phase 2 goal policy and Phase 4 period_view (call + result use, not bare import).
- Phase 2–4 service methods exposed by BOTH Phase 5 CLI and HTTP API; CLI/API call identical `TodoService` methods (CORE-03).
- Dual-store equivalence proven at runtime: `period_view::parity_in_memory_vs_persistent`, `period_view::parity_goal_task_goal_cross_store`, `date_view::parity_in_memory_vs_persistent`.
- Core E2E flow (create period goal → link dated task → see in agenda/date-range + period tree) completes on both surfaces.
- Tests: integration 62/62, unit 49/49, lib 2/2; e2e 37/38 (the single failure is the pre-existing, out-of-scope `cli::init_loads_todo_engine_home_from_dotenv`).

## Tech Debt (non-blocking)

1. **Nyquist coverage is partial** — `04` and `05` COMPLIANT; `02`, `03`, `04.1` PARTIAL; `01` has no VALIDATION.md. All phases passed VERIFICATION and all real test suites are green; the formal Nyquist Dimension records are incomplete. Optional retroactive close: `/gsd-validate-phase <N>`.
2. **Code review WR-01 (Phase 5)** — API `approve`/`complete` accept no request body (`reason = None` hardcoded) while the CLI accepts `--reason`. Inert today (service binds `_reason` unused) but a latent silent audit-reason drop; outside SURF parity scope. Tracked in `05-REVIEW.md`.
3. **SUMMARY frontmatter bookkeeping** — LINK-01/LINK-02/VIEW-01 are not listed in Phase 2 per-plan SUMMARY `requirements_completed`, though `02-VERIFICATION.md` confirms them SATISFIED. Cosmetic traceability-record gap only.

## Re-audit after merging `feature/workspace-item-table-editing`

After the initial audit passed, the `feature/workspace-item-table-editing` branch was merged
into main (merge commit `e00bb3b`). That branch forked before Phase 5 and added a parallel
frontend (workspace item-table UI) plus backend API changes (its own goal-propose route, goal
filters through the item API, mock-db seed scripts). Merge handling:

- **Conflicts (api/dto.rs, api/handlers.rs):** resolved by keeping the Phase 5 supersets
  (`GoalProposeBody{parent_id,note,actor}`; `AgendaQuery`/`DateRangeQuery`/`PeriodQuery`/`PeriodView` imports).
- **Auto-merge defect (api/mod.rs):** both branches registered `.route("/goals/propose")`; git
  kept both, axum panicked on the duplicate at router build (every API e2e failed). Fixed by
  removing the duplicate — one registration remains.

Re-audit (integration checker on the merged tree): **0 blockers, 1 non-regression warning.**
All 17 requirements remain WIRED — no regression. The feature branch's "goal filters through
item API" composes the existing `ListFilter` + `service.list_items` + shared `apply_list_filter`
(both stores), so CORE-01 (single mutation path) and CORE-03 (shared view logic) hold. Phase 5
CLI surface intact. Backend tests on the merged tree: unit 49/49, integration 62/62, e2e 38/39
(sole failure = the out-of-scope deferred dotenv test). Frontend not test-verified (node_modules
not installed; out of v1.0 backend scope).

The 1 warning is the CLI-`list` vs API-`/items` filter-exposure asymmetry — pre-existing, not a
merge regression, CORE-03 still satisfied (see tech_debt).

## Verdict

All 17 v1 requirements satisfied, all 6 phases verified, cross-phase integration and the core
E2E flow proven on both surfaces — and re-confirmed after the `feature/workspace-item-table-editing`
merge. The accumulated tech debt is minor and non-blocking.
**Milestone v1.0 is ready to complete.**
