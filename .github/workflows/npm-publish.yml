name: Publish npm Package

on:
  workflow_dispatch:
  push:
    tags:
      - "npm-v*.*.*"

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    name: Publish @shings/raven
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: npm/raven
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"
          package-manager-cache: false
      - name: Verify package identity
        run: test "$(node -p "require('./package.json').name")" = "@shings/raven"
      - name: Test wrapper
        run: npm test
      - name: Publish to npm
        run: npm publish --access public
